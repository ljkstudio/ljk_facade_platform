# 30. 복곡면 블랭크 전개 v1 — `UFv1 Material` / `UFv1 Flatten` 사용 설명서

> 복곡면 패널 한 장을 **성형 전 평판(블랭크)** 으로 펴고, 그 결과가 실제로 만들 수 있는
> 것인지 판정하는 Grasshopper 컴포넌트 둘입니다.
>
> 관련 문서: 아키텍처 원칙은 [`00_platform_overview.md`](00_platform_overview.md),
> 설계 근거는 `docs/superpowers/specs/2026-08-14-unfold-blank-development-design.md`.
> 이 문서는 **지금 실제로 돌아가는 것**과 그 한계만 다룹니다.

---

## 1. 무엇을 하는가

곡면 패널을 판재로 만들려면 "이 곡면을 펴면 어떤 모양의 평판이 되는가"를 알아야 합니다.
곡면이 원통처럼 가전개면(developable)이면 늘어남 없이 정확히 펴지지만, 돔이나 안장처럼
이중곡률이면 **어떻게 펴도 늘어나거나 압축됩니다.** 이 컴포넌트는 그 늘어남·압축을
최소로 하는 평면 배치를 찾고, 얼마나 남았는지를 숫자로 말합니다.

```
곡면(Brep) 한 장
   │
   ├─ ① 메쉬화        edge_mm 목표 변 길이로 삼각망 (Rhino)
   ├─ ② 위상 검사      구멍·비다양체·닫힘·조각 나뉨을 앞에서 거부
   ├─ ③ 초기 배치      최적평면 투영 → 접히면 Tutte 매립으로 대체
   ├─ ④ ARAP 평탄화    local-global 반복 (요소별 최근접 회전 ↔ 라플라시안 풀이)
   ├─ ⑤ 판정          요소별 σ → 주름·찢어짐·뒤집힘·수렴
   └─ ⑥ 재단선        경계 단순화 → 바깥 오프셋(여유) → 자기교차 검사
   │
   ▼
평면 메쉬 + 재단 외곽선 + σ 색칠 + 판정 목록
```

**계산은 Rhino 없이 도는 순수 파이썬(`unfold/src/`)에 있고, Rhino 의존은 `rhino_io.py`
한 파일과 GH 어댑터 둘뿐입니다.** 그래서 `python -m pytest unfold/tests` 로 116개
테스트가 Rhino 없이 돕니다.

---

## 2. 컴포넌트가 둘인 이유

| 컴포넌트 | 하는 일 |
|---|---|
| **UFv1 Material** | 재료 물성을 객체 하나로 묶는다. 무엇이 형상을 바꾸고 무엇이 기록일 뿐인지 말한다 |
| **UFv1 Flatten** | 전개 본체. 곡면 하나 → 평면 메쉬 · 재단선 · 판정 |

Flatten 의 입력을 물성 개수만큼 늘리는 대신 `props` 객체 하나를 물립니다
(AMv1 을 Base/Check/Play 로 쪼갠 것과 같은 이유).
**Material 은 선택입니다** — 안 물리면 기본 물성으로 돌고, 찢어짐은 "미판정"으로 남습니다.

---

## 3. 준비

### 3.1 캔버스에 올리기

컴포넌트는 저장소 도구가 만듭니다. 캔버스를 손으로 만들지 않습니다.

```bash
# Rhino 8 + Grasshopper 를 띄우고, Rhino 명령창에 mcpstart 를 입력한 뒤
python unfold/tools/build_uf_components.py
```

캔버스에 NickName 이 정확히 `UFv1 Material` / `UFv1 Flatten` 인 **Python 3 Script**
컴포넌트가 있어야 하고, 도구가 입출력 파라미터·타입 힌트·코드를 한 번에 맞춥니다.
`gh_scripts/*.py` 를 고칠 때마다 다시 돌립니다 — **코드는 `.gh` 이진 안에 박히므로
저장소만 고치면 캔버스는 옛 코드로 돕니다.**

두 컴포넌트는 **`unfold/grasshopper/UFv1.gh`** 에 시험 하네스(경로 패널 · 곡면 참조 ·
슬라이더 · 물성)와 함께 들어 있습니다. AMv1 캔버스와 분리돼 있으므로 이 파일만 열면
됩니다.

```bash
# 열기 / 저장 — 저장 후 다시 읽어 객체·연결·코드 길이를 센다
python ~/.claude/skills/rhino-bridge/scripts/rhino_bridge.py run-file \
    unfold/tools/save_uf_document.py
```

### 3.1b 리본에서 꺼내 쓰기

두 컴포넌트는 Grasshopper 리본 **`LJKS` 탭의 `Unfold` 그룹**에 있습니다
(AMv1 컴포넌트 7개는 같은 탭의 `AMv1` 그룹).

```bash
python unfold/tools/make_uf_user_objects.py
```

**`.ghuser` 는 스냅샷입니다** — 컴포넌트를 코드까지 통째로 복사해 담습니다.
그래서 `gh_scripts/*.py` 를 고치면 갱신 경로가 **둘로 갈립니다**:

| 무엇 | 도구 |
|---|---|
| 캔버스에 이미 있는 것 | `build_uf_components.py` (+ `apply_uf_param_docs.py`) |
| 리본에서 새로 꺼낼 것 | `make_uf_user_objects.py` |

한쪽만 돌리면 팔레트에서 꺼낸 컴포넌트가 옛 코드로 돕니다. 캔버스에 있던 것은
멀쩡해서 원인을 찾기가 특히 어렵습니다.

### 3.2 `platform_path`

두 컴포넌트의 첫 입력은 저장소 루트 폴더 경로입니다. 패널(Panel)에 경로를 적어 물립니다.

```
C:\Users\leeja\Documents\dev\26_AdaptiveMold_development
```

이 경로로 `unfold/src` 를 `sys.path` 에 얹습니다. 경로를 컴포넌트 코드에 하드코딩하지
않는 이유는 다른 PC 에서 통째로 죽기 때문입니다.

### 3.3 numpy — 첫 줄 한 줄이 20배다

`UFv1_Flatten.py` 첫 줄의 `# r: numpy` 가 이 컴포넌트에서 값이 가장 큰 한 줄입니다.
Rhino 8 의 CPython 3.9 는 이 선언이 없으면 numpy 를 못 봅니다. **없는 게 아니라
선언하지 않으면 안 보이는 것입니다.**

| 환경 | 돔 2,049정점 전개 |
|---|---|
| `# r: numpy` 있음 (numpy 2.0.2) | **0.50 s** |
| 없음 (순수 파이썬) | 10.28 s |

numpy 를 못 끌어오는 환경에서도 계산은 됩니다. 그때는 `info` 에
`numpy 없음 — 순수 파이썬 (수십 배 느리다)` 가 찍히고, 정점 상한이 자동으로 낮아집니다.

---

## 4. `UFv1 Material` — 입력과 출력

### 입력

| 이름 | 형 | 단위 | 등급 | 뜻 |
|---|---|---|---|---|
| `platform_path` | str | — | — | 저장소 루트 (필수) |
| `name` | str | — | 기록 | 재료 이름 (예: `AL-5052`) |
| `thickness` | float | mm | **기록** | 두께. **형상에는 영향 없음** (막 모델) |
| `elong_max` | float | **비율** | **판정** | 균일연신률. 12% 는 `12` 가 아니라 `0.12` |
| `wrinkle_penalty` | float | — | **형상** | σ>1 쪽 벌점. `1.0` 이 순수 ARAP |
| `source` | str | — | 기록 | 이 숫자들이 어디서 왔는가 |

### 출력

| 이름 | 뜻 |
|---|---|
| `props` | Flatten 의 `props` 에 물린다 |
| `info` | 무엇이 형상을 바꿨고 무엇이 기록일 뿐인지 |

### 등급이 있는 이유

**ARAP 에너지에는 탄성계수도 푸아송비도 들어가지 않습니다.** E 를 곱해도 최소점이
그대로라 형상이 안 바뀝니다. 그런 값을 입력으로 받아 두면 돌려도 아무 일이 안 일어나는
**가짜 손잡이**가 됩니다 — 사용자는 조정하고 있다고 믿는데 실제로는 아무것도 안 합니다.
그래서 `info` 가 항목마다 등급을 말합니다:

```
재료: AL-5052
  [형상] wrinkle_penalty = 1  (1.0 — 순수 ARAP, 벌점 없음)
  [판정] elong_max = 12.0%
  [기록] 두께 3 mm — 막 모델이라 **형상에는 영향 없음**
  [기록] 출처: 사내 시험성적서 2026-03
```

E·ν·항복곡선·FLD·r 값은 **입력조차 만들지 않았습니다.** v2 소성 모델과 함께 옵니다.

**프리셋은 없습니다.** 알루미늄 균일연신률을 출처와 함께 댈 수 없기 때문입니다.
근거 없는 기본값을 넣으면 아무도 안 고치고 그대로 씁니다.

**잘못된 물성은 하류로 안 갑니다.** `wrinkle_penalty < 1`, 음수 두께, 비율 아닌 연신률은
`props` 를 `None` 으로 만들고 이유를 `info` 에 적습니다. Flatten 은 기본 물성으로 돌면서
"props 가 없어 기본 물성으로 돌았다"를 경고에 남깁니다.

---

## 5. `UFv1 Flatten` — 입력과 출력

### 입력

| 이름 | 형 | 기본 | 뜻 |
|---|---|---|---|
| `platform_path` | str | — | 저장소 루트 (필수) |
| `srf` | Brep | — | 전개할 곡면 **한 장** |
| `props` | object | 없음 | `UFv1 Material` 의 출력. 없으면 기본 물성 |
| `edge_mm` | float | 40 | 목표 요소 크기 mm. 작을수록 정밀·느림 |
| `allow_mm` | float | 15 | 재단 여유 mm |
| `iters` | int | 30 | ARAP 최대 반복 |
| `max_verts` | int | 자동 | 정점 상한. 비우면 이 환경 속도로 계산 (§8) |
| `run` | bool | — | 계산 스위치 |

**`run` 토글이 있는 이유**: GH 스크립트 컴포넌트는 Rhino UI 스레드에서 돕니다.
2초 걸리는 계산은 2초 동안 Rhino 를 얼립니다. 슬라이더를 만질 때마다 도는 것을 막습니다.
`run` 이 꺼져 있으면 `warn` 에 **"미판정이지 통과가 아니다"** 가 찍힙니다.

### 출력

| 이름 | 형 | 뜻 |
|---|---|---|
| `flat` | Mesh | 평탄화된 평면 메쉬 (z=0) |
| `blank` | Curve | 재단 외곽선. 닫힌 폴리라인 |
| `strain` | Mesh | σ 색칠 — **파랑 σ<1**(성형에서 인장), 흰색 σ=1, **빨강 σ>1**(주름 위험). σ가 1에서 ±0.20 벗어나면 만색 |
| `warn` | str | 판정 한 줄씩. **통과한 것도 적는다** |
| `info` | str | 요약 — 메쉬 크기·소요 시간·σ 범위·면적·재단선·물성 |

`flat` 과 `strain` 은 같은 자리에 겹칩니다. 색을 보려면 `flat` 의 프리뷰를 끄세요.

---

## 6. 결과 읽는 법

### 6.1 σ 부호 규약 — 이것만 헷갈리지 않으면 됩니다

**σ = (평면에서의 길이) / (3D 곡면에서의 길이)**

| | 평탄화에서 | **성형에서** | 뜻 |
|---|---|---|---|
| **σ < 1** | 줄어듦 | **늘어남** (인장) | 연신 한계까지는 정상. 넘으면 찢어진다 |
| **σ > 1** | 늘어남 | **압축됨** | **주름 위험** |

성형 변형률 = `1/σ − 1`. `info` 의 "최대 성형 변형률"은 가장 큰 인장이고,
주름 경고의 "최대 압축 %"는 가장 큰 압축입니다.

### 6.2 `warn` 의 네 판정

**빈 경고 목록이 "검사했고 괜찮다"인지 "검사를 안 했다"인지 구별되지 않으면 판정은
쓸모가 없습니다.** 그래서 통과한 것도 적고, 검사하지 않은 것은 **미판정**으로 남깁니다.

| 판정 | 통과 | 경고 | 미판정 |
|---|---|---|---|
| **주름** | 모든 요소가 σ ≤ 1 | 압축되는 요소 수 + **최대 압축 %** | 요소가 전부 뒤집혀 잴 수 없음 |
| **찢어짐** | 최대 변형률 < `elong_max` | 한계를 넘는 요소 수 | **`elong_max` 가 없음** |
| **뒤집힘** | 뒤집힌 요소 없음 | 뒤집힌 요소 수 — 그 요소의 변형률은 못 믿는다 | — |
| **수렴** | 반복 안에서 수렴 | 상한에서 멈춤 — 반복을 늘려야 한다 | — |

**주름 경고는 개수가 아니라 크기로 읽습니다.** 가전개면인 원통에서도 수치 잔차 때문에
요소 절반이 σ>1 로 걸리는데, 실제 압축은 0.001% 입니다. 그래서 문구가 크기를 함께 말합니다:

```
[경고] 주름: 성형 중 압축되는 요소 3120개 (전체 6281개). 최대 압축 0.001% (σ = 1.000014)   ← 무시해도 된다
[경고] 주름: 성형 중 압축되는 요소 4968개 (전체 11592개). 최대 압축 6.608% (σ = 1.070752)  ← 진짜 주름이다
```

### 6.3 재단선의 보증

```
경계 128점 → 단순화 102점 (허용 1.00 mm, 코너 0개 고정)
여유 15.00 mm + 단순화 허용 1.00 mm = 오프셋 16.00 mm
최소 여유 실측 15.005 mm
```

단순화가 안쪽으로 최대 1 mm 파고들 수 있으므로 오프셋을 그만큼 더 줍니다. 따라서
**재단선은 평탄화 경계 바깥으로 항상 `allow_mm` 이상 떨어져 있습니다.** 재료를 조금 더
쓰는 대신 과소재단이 구조적으로 불가능해집니다.

**보증이 깨지는 유일한 경우는 뾰족한 꼭짓점입니다.** 내각이 약 23.1° 미만이면 마이터
제한이 걸려 실측 여유가 요구치보다 가까워집니다(실측: 10 mm 요구에 6.6 mm, 내각 14.2°).
v1 은 기하를 고치지 않고 **재서 `notes` 에 알립니다.** 조용히 깨지는 것만은 막습니다.

---

## 7. 시험 곡면 5종 — 이 도구를 믿어도 되는 근거

`adaptive_mold/grasshopper/test_panels.3dm` 의 패널로 실측 (2026-08-14, Rhino 8.33, 이 PC):

| 곡면 | 정점 | 계산 | σ 범위 | 최대 성형 변형률 | 면적비 | 주름 |
|---|---|---|---|---|---|---|
| `panel_flat` | 2,952 | 0.15 s | 1.000000 ~ 1.000000 | 0.00% | 1.0000 | 통과 |
| `panel_tilt10` | 2,326 | 0.12 s | 1.000000 ~ 1.000000 | 0.00% | 1.0000 | 통과 |
| `panel_cylinder` | 3,238 | 2.05 s | 0.999987 ~ 1.000014 | 0.00% | 1.0000 | 압축 0.001% |
| `panel_saddle` | 1,024 | 0.40 s | 0.991905 ~ 1.002921 | 0.82% | 1.0000 | 압축 0.291% |
| `panel_dome` | 5,889 | 1.13 s | 0.966778 ~ 1.070752 | 3.42% | 0.9984 | **압축 6.608%** |

읽는 법:

- **평면·10° 기울기·원통은 가전개면**이므로 σ 가 1 이어야 합니다. 실측이 1.4e-5 안에서 맞습니다.
- **돔과 안장만 실제 변형이 납니다.** 돔은 인장 3.42% · 압축 6.6% 로 이론과 맞습니다.
- 다섯 경우 모두 **평탄화 메쉬의 정점이 재단선 안에 하나도 빠짐없이** 들어갔습니다.

같은 시험은 언제든 다시 돌릴 수 있습니다:

```bash
python unfold/tools/wire_uf_harness.py panel_dome edge=40 elong=0.12 name=AL-5052
python unfold/tools/run_uf_test.py
```

---

## 8. 속도와 "Rhino 가 얼어붙는" 문제

계산은 Rhino UI 스레드에서 돕니다. **계산 시간 = 화면이 멈춰 있는 시간**입니다.
그래서 `info` 첫 줄에 걸린 시간과 엔진을 적습니다:

```
계산 1.13 s — 그동안 Rhino 는 멈춰 있다 (numpy 2.0.2)
```

### 정점 상한은 개수가 아니라 시간 예산

실측 (돔, Rhino 8.33 py39):

| | 2,049정점 | 5,889정점 | 16,385정점 |
|---|---|---|---|
| numpy 있음 | 0.50 s | 1.44 s | 6.14 s |
| numpy 없음 | 10.28 s | — | — |

예산은 **3초**이고, 상한은 백엔드 속도에서 거꾸로 계산합니다 —
numpy 면 약 8,100정점, 순수 파이썬이면 약 600정점. 넘으면 계산하지 않고 이렇게 말합니다:

```
[경고] 정점이 16385개다 — 이 환경에서 약 6초 걸린다 (상한 8108개, 예산 3초).
계산하는 동안 Rhino 는 멈춰 있다. 요소 크기(edge_mm)를 키우거나,
오래 걸려도 좋으면 max_verts 를 올려라.
```

### `edge_mm` 고르는 법

1. **먼저 크게** (60~80) 잡아 형상과 판정을 봅니다 — 1초 안에 돕니다.
2. σ 와 재단선이 말이 되면 **줄여서** (30~40) 다시 돌립니다.
3. 상한에 걸리면 메시지의 예상 시간을 보고 `edge_mm` 을 키우거나 `max_verts` 를 올립니다.

요소를 절반으로 줄이면 정점은 약 4배, 시간도 약 4배입니다.

---

## 9. v1 이 하지 않는 것

| 항목 | 상태 |
|---|---|
| **구멍 있는 패널** | 거부. 경계 루프가 2개 이상이면 계산하지 않는다 |
| **여러 조각 · 닫힌 메쉬** | 거부. 한 장씩 넣어야 한다 |
| **소성·이방성·두께 효과** | 없음. 막 모델이고 `thickness` 는 기록일 뿐 |
| **스트립 분할** (전개가 안 되는 곡면을 쪼개기) | 없음 |
| **네스팅(자재 배치)** | 없음. 게다가 정사각 패널은 결과가 45° 돌아 나온다\* |
| **오목 모서리의 오프셋 자기교차** | 고치지 않고 **검출해서 알린다** |
| **뾰족한 꼭짓점(내각 23.1° 미만)의 여유 보증** | 성립하지 않는다. 재서 알린다 (§6.3) |
| **`wrinkle_penalty > 1` 의 수렴성** | 증명하지 않았다. 기본값 1.0 이 증명된 경로다 |

\* 결과는 주축이 수평이 되도록 정렬하는데, **정사각형은 공분산이 등방이라 주축이 없습니다.**
그래서 1200×1200 패널이 bbox 1697×1697 로 45° 돌아 나옵니다. 계산은 맞지만 시트 활용률에는
불리합니다. 최소면적 외접사각형 정렬은 아직 넣지 않았습니다.

---

## 10. 문제 해결

| 증상 | 원인 | 해결 |
|---|---|---|
| 컴포넌트가 아예 안 돈다. 출력이 전부 비었고 `Input parameter X failed to collect data` 경고 | 입력이 필수(Optional 아님)로 잡혔다 | `build_uf_components.py` 를 다시 돌린다 (도구가 `Optional` 을 세운다) |
| 저장소를 고쳤는데 결과가 그대로다 | 코드가 `.gh` 이진에 박혀 있다 | `build_uf_components.py` 로 다시 밀어 넣는다 |
| `run` 을 켰는데 결과가 없다 | 정점 상한에 걸렸다 | `info` 의 예상 시간을 보고 `edge_mm` ↑ 또는 `max_verts` ↑ |
| 계산이 수십 초 걸린다 | numpy 를 못 쓰고 있다 | `info` 에 "numpy 없음"이 있는지 확인. 스크립트 첫 줄 `# r: numpy` |
| 원통·평면인데 주름 경고가 뜬다 | 수치 잔차 (σ 가 1을 1e-5 수준으로 넘음) | 문구의 **최대 압축 %** 를 본다. 0.01% 미만이면 무시 |
| "미수렴" 이 뜬다 | 반복 상한에서 멈췄다 | `iters` 를 올린다. 에너지가 이미 바닥이면 결과는 쓸 만하다 |
| 파라미터 툴팁이 비어 있다 | **`.gh` 가 설명을 보관하지 못한다**(Rhino 재시작마다), 또는 방금 코드를 push 했다 | `python unfold/tools/apply_uf_param_docs.py` (§11) |
| `props` 를 물렸는데 기본 물성으로 돈다 | 물성 검증에 걸려 `props` 가 `None` 이 됐다 | `UFv1 Material` 의 `info` 에서 `[경고]` 줄을 읽는다 |

---

## 11. 개발자용 — 도구와 테스트

| 도구 | 하는 일 |
|---|---|
| `unfold/tools/build_uf_components.py` | 파라미터·힌트·코드를 캔버스에 밀어 넣는다 (멱등) |
| `unfold/tools/apply_uf_param_docs.py` | 파라미터 툴팁을 밀어 넣는다. `--check` 로 대조만 |
| `unfold/tools/make_uf_user_objects.py` | 리본 `LJKS > Unfold` 에 `.ghuser` 로 등록 (아이콘 포함) |
| `unfold/tools/save_uf_document.py` | `UFv1.gh` 열기·저장 + 다시 읽어 검증 (`run-file` 로) |
| `unfold/tools/split_uf_document.py` | 다른 문서에 섞인 UF 덩어리를 떼어낸다 (`run-file` 로) |
| `unfold/tools/wire_uf_harness.py` | 시험용 입력을 물린다 — 곡면 참조·경로·슬라이더·물성 |
| `unfold/tools/run_uf_test.py` | 한 번 돌리고 **출력값과 런타임 메시지로** 판정한다 |
| `unfold/tools/profile_in_rhino.py` | Rhino 안에서 단계별 소요 시간을 잰다 |
| `unfold/tools/run_rhino_check.py` | `rhino_io.py` 의 Rhino 경계면 검사 |

```bash
python -m pytest unfold/tests -q      # 116 passed — Rhino 없이 돈다
```

### 툴팁은 두 번 사라진다

파라미터 설명의 정본은 `unfold/gh_scripts/param_docs.py` 입니다(설명 23개).
캔버스에서 직접 적지 않습니다 — `.gh` 이진 안에만 남아 git diff 가 안 됩니다.

| 언제 사라지는가 | 대응 |
|---|---|
| **코드를 push 할 때마다** | 빌더가 파라미터를 다시 만들면서 설명이 기본값으로 돌아간다 (실측: 23개 중 21개) |
| Rhino 를 다시 켤 때 (구형 GhPython 기준) | 문서를 열면 한 번 돌린다 |

> **[정정 2026-08-14]** "`.gh` 는 설명을 보관하지 못한다"는 AMv1(구형 GhPython)
> 기준이고 **재시작 후** 측정된 것입니다(125개 중 120개 소실). UFv1 을 별도 `.gh` 로
> 저장했다가 다시 열었을 때는 **23개가 전부 남아 있었습니다** — 새 Script 컴포넌트는
> 최소한 같은 세션 안에서는 파일이 설명을 들고 옵니다. **재시작까지 살아남는지는
> 아직 미검증**이라 절차(문서를 열면 한 번 돌린다)는 그대로 둡니다.

```bash
python unfold/tools/build_uf_components.py     # 코드·파라미터
python unfold/tools/apply_uf_param_docs.py     # ← 반드시 이어서. 빌더가 이 줄을 출력한다
python unfold/tools/apply_uf_param_docs.py --check   # 0 차이가 정상 상태
```

**이 손실이 위험한 이유는 아무 흔적이 없기 때문입니다.** 코드 push 는 성공으로
보고되고, 캔버스도 정상이고, 계산 결과도 맞습니다. 잃은 것은 마우스를 올렸을 때만
보이므로 며칠 뒤에 발견됩니다. 그래서 `--check` 로 상시 대조합니다.

설명은 `Description` 과 `ToolTip` **두 속성 모두**에 넣습니다. 새 Script 컴포넌트의
파라미터에는 구형에 없던 `ToolTip` 이 따로 있고, 화면이 어느 쪽을 그리는지는
저장소 쪽에서 단정할 수 없기 때문입니다.

**캔버스 모양은 근거가 아닙니다.** 판정은 `RuntimeMessages` 와 출력값으로 합니다.
자세한 절차와 함정은 `gh-component-dev` / `rhino-bridge` 스킬, 그리고 `docs/journal/` 에 있습니다.

---

**실측 기준일**: 2026-08-14 / Rhino 8.33.26188.13001 / numpy 2.0.2 / Python 3.9.10
