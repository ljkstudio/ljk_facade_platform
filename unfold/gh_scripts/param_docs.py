# -*- coding: utf-8 -*-
"""UFv1 컴포넌트 파라미터 설명(툴팁)의 정본.

**왜 저장소에 두는가:** 파라미터 설명은 `.gh` 이진 안에만 저장되고, 게다가
**`.gh` 는 그걸 제대로 보관하지도 못한다** — 저장·재시작하면 사라진다(실측:
AMv1 에서 125개 중 120개 소실). 캔버스에서 직접 적으면 git diff 가 안 되고,
컴포넌트를 다시 만들면 통째로 없어진다. 여기를 정본으로 두고
`unfold/tools/apply_uf_param_docs.py` 로 밀어넣는다.

**두 번 사라진다:**
  ① Rhino 를 다시 켤 때마다 (`.gh` 가 못 들고 있어서)
  ② 코드를 push 할 때마다 — `build_uf_components.py` 가 파라미터를 다시 만들면
     설명이 기본값(파라미터 이름)으로 돌아간다
→ **빌더를 돌린 뒤에는 항상 이 도구를 이어서 돌린다.**

**형식 주의:** Grasshopper 툴팁은 평문이다. 마크다운(**굵게** 등)은 기호가
그대로 보이므로 쓰지 않는다. 줄바꿈(\\n)은 그대로 반영된다.

설명을 쓰는 기준은 하나다 — **마우스를 올린 사람이 다음에 무엇을 할지 정할 수
있어야 한다.** 단위·기본값·틀렸을 때의 증상을 적는다. 이름을 풀어 쓰기만 한
설명(`edge_mm: 변 길이`)은 없느니만 못하다.
"""

PARAM_DOCS = {
    # ─────────────────────────────────────────────────────────────────
    "UFv1 Material": {
        "inputs": {
            "platform_path":
                "저장소 루트의 절대경로. 이 아래 unfold/src 를 sys.path 에 넣어\n"
                "계산 모듈을 불러온다.\n"
                "예: C:\\Users\\leeja\\Documents\\dev\\26_AdaptiveMold_development\n"
                "필수 — 비어 있으면 props 가 나오지 않는다.",
            "name":
                "재료 이름. 예: AL-5052, STS304\n"
                "[기록] 계산에 쓰이지 않는다. 결과를 나중에 알아보기 위한 것이다.",
            "thickness":
                "판재 두께 (mm).\n"
                "[기록] 형상에는 영향이 없다 — v1 은 막(membrane) 모델이라\n"
                "두께가 평탄화 결과를 바꾸지 않는다. 그래도 받아 두는 이유는\n"
                "결과에 어떤 판재를 전제했는지 남기기 위해서다.\n"
                "0 이하면 물성 전체가 거부된다.",
            "elong_max":
                "균일연신률. **비율**이다 — 12% 는 12 가 아니라 0.12.\n"
                "[판정] 찢어짐 판정의 한계값. 최대 성형 변형률이 이 값을 넘는\n"
                "요소를 경고한다.\n"
                "비워 두면 찢어짐은 '통과'가 아니라 '미판정'으로 남는다.\n"
                "0 이하거나 1 을 넘으면 거부된다(퍼센트를 그대로 넣은 실수).",
            "wrinkle_penalty":
                "σ>1 (성형에서 압축되는) 요소에 주는 벌점. 기본 1.0.\n"
                "[형상] 이 컴포넌트에서 결과 형상을 바꾸는 유일한 값이다.\n"
                "1.0 은 순수 ARAP — 에너지가 단조 감소한다는 것이 보장된 경로다.\n"
                "1 보다 크게 하면 주름을 줄이는 쪽으로 당기지만 **수렴성이\n"
                "증명되지 않았다.** 결과가 요동치면 1.0 으로 되돌릴 것.\n"
                "1 미만은 부호가 뒤집혀 주름을 키우므로 거부된다.",
            "source":
                "이 숫자들이 어디서 왔는가. 예: 사내 시험성적서 2026-03\n"
                "[기록] 비어 있어도 계산은 되지만 info 에 '출처 미기재'가 남는다.\n"
                "출처 없는 연신률은 나중에 검증할 수가 없다.",
        },
        "outputs": {
            "props":
                "물성 객체. UFv1 Flatten 의 props 에 물린다.\n"
                "검증에 걸리면 None 이 나온다 — 잘못된 물성을 하류로 흘려보내지\n"
                "않기 위해서다. 그때 info 의 [경고] 줄을 읽을 것.",
            "info":
                "무엇이 형상을 바꿨고 무엇이 기록일 뿐인지.\n"
                "[형상] / [판정] / [기록] 세 등급으로 적힌다. ARAP 에너지에는\n"
                "탄성계수도 푸아송비도 들어가지 않으므로, 돌려도 아무 일이\n"
                "안 일어나는 '가짜 손잡이'를 만들지 않으려고 등급을 명시한다.",
            "out":
                "표준출력(print). 정상 동작에서는 비어 있다.",
        },
    },
    # ─────────────────────────────────────────────────────────────────
    "UFv1 Flatten": {
        "inputs": {
            "platform_path":
                "저장소 루트의 절대경로. 이 아래 unfold/src 를 sys.path 에 넣어\n"
                "계산 모듈을 불러온다.\n"
                "예: C:\\Users\\leeja\\Documents\\dev\\26_AdaptiveMold_development\n"
                "필수 — 비어 있으면 아무것도 계산되지 않는다.",
            "srf":
                "전개할 곡면 한 장 (Surface 또는 Brep).\n"
                "필수. 조건이 좁다 — 구멍 없는 원판 한 장만 편다.\n"
                "구멍이 있거나(경계 루프 2개 이상), 닫혀 있거나, 조각이\n"
                "여럿이면 계산하지 않고 위상 경고를 낸다. 한 장씩 넣을 것.",
            "props":
                "UFv1 Material 의 props 출력.\n"
                "선택 — 안 물리면 기본 물성으로 돌고, 찢어짐은 미판정으로\n"
                "남는다(경고에 그렇게 적힌다).",
            "edge_mm":
                "메쉬 목표 변 길이 (mm). 기본 40.\n"
                "작을수록 결과가 정밀해지지만 정점이 제곱으로 늘고 시간도\n"
                "그만큼 든다 — 절반으로 줄이면 대략 4배다.\n"
                "먼저 60~80 으로 형상과 판정을 보고, 말이 되면 30~40 으로\n"
                "다시 돌리는 순서를 권한다.",
            "allow_mm":
                "재단 여유 (mm). 기본 15.\n"
                "재단선은 평탄화 경계보다 항상 이 값 이상 바깥에 놓인다.\n"
                "경계 단순화가 안쪽으로 파고들 수 있어 그만큼(1mm)을 더해\n"
                "오프셋하므로, 실측 여유는 요구치보다 조금 크다.\n"
                "예외: 내각 23.1도 미만의 뾰족한 꼭짓점에서는 보증이 깨진다.\n"
                "그때는 info 에 실측 여유가 굵게 적힌다.",
            "iters":
                "ARAP local-global 최대 반복. 기본 30.\n"
                "에너지 상대변화가 1e-6 아래로 떨어지면 그 전에 멈춘다.\n"
                "'미수렴' 경고가 뜨면 여기를 올린다. 다만 에너지가 이미\n"
                "바닥이면(원통에서 3.4e-05) 결과는 그대로 쓸 만하다.",
            "max_verts":
                "정점 상한. **비워 두면 이 환경의 속도로 자동 계산한다.**\n"
                "이 컴포넌트는 Rhino UI 스레드에서 돌기 때문에 계산 시간이\n"
                "곧 화면이 멈춰 있는 시간이다. 그래서 상한을 개수가 아니라\n"
                "시간 예산(3초)에서 거꾸로 잡는다 — numpy 가 붙으면 약 8100개,\n"
                "순수 파이썬이면 약 600개.\n"
                "넘으면 계산하지 않고 '약 몇 초 걸린다'를 경고에 적는다.\n"
                "오래 걸려도 좋으면 여기에 직접 큰 값을 넣으면 그 값이 이긴다.",
            "run":
                "계산 스위치. 켜야 돈다.\n"
                "이 컴포넌트는 Rhino UI 스레드에서 도므로, 슬라이더를 만질\n"
                "때마다 다시 계산하면 그때마다 Rhino 가 멈춘다.\n"
                "꺼져 있을 때의 빈 결과는 '통과'가 아니라 '미판정'이다 —\n"
                "warn 에 그렇게 적힌다.",
        },
        "outputs": {
            "flat":
                "평탄화된 평면 메쉬 (z=0).\n"
                "결과는 무게중심이 원점에, 주축이 +X 가 되도록 정렬된다.\n"
                "정사각형처럼 주축이 없는 형상은 방향이 자유롭다 — 1200x1200\n"
                "패널이 45도 돌아 나오는 것은 그 때문이고, 계산은 맞다.",
            "blank":
                "재단 외곽선. 닫힌 폴리라인.\n"
                "평탄화 경계에서 allow_mm 이상 바깥으로 떨어져 있다.\n"
                "오목 모서리에서 자기교차할 수 있는데, v1 은 고치지 않고\n"
                "검출해서 info 에 알린다 — 그 경우 손으로 확인할 것.",
            "strain":
                "σ 색칠 메쉬. flat 과 같은 자리에 겹치므로 색을 보려면\n"
                "flat 쪽 프리뷰를 끈다.\n"
                "파랑 = σ<1 (성형에서 늘어남), 흰색 = σ=1 (변형 없음),\n"
                "빨강 = σ>1 (성형에서 압축 = 주름 위험).\n"
                "σ 가 1 에서 0.20 벗어나면 만색이 된다.",
            "warn":
                "판정 목록. 한 줄에 하나씩 (\\n 으로 이어져 있다).\n"
                "주름 / 찢어짐 / 뒤집힘 / 수렴 네 가지를 통과·경고·미판정으로\n"
                "적는다. **통과한 것도 적는다** — 빈 목록이 '검사했고 괜찮다'\n"
                "인지 '검사를 안 했다'인지 구별되지 않으면 판정이 쓸모없다.\n"
                "주름 경고는 개수가 아니라 '최대 압축 %' 로 읽을 것.\n"
                "가전개면인 원통에서도 수치 잔차로 요소 절반이 걸리는데\n"
                "실제 압축은 0.001% 다.",
            "info":
                "요약. 메쉬 크기 / 계산에 걸린 시간과 엔진 / 반복과 수렴 /\n"
                "초기 배치 방법 / σ 범위 / 최대 성형 변형률 / 3D 대비 평면\n"
                "면적비 / 재단선 점 수와 실측 여유 / 물성 등급.\n"
                "'계산 N초' 는 그동안 Rhino 가 멈춰 있던 시간이다.\n"
                "'numpy 없음' 이 보이면 스크립트 첫 줄의 '# r: numpy' 가\n"
                "빠진 것이다 — 같은 메쉬가 20배 느려진다.",
            "out":
                "표준출력(print). 정상 동작에서는 비어 있다.",
        },
    },
}
