# AMv1 `.gha` 포팅 — M2b 구현 계획 (Core 포팅 A~D)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 파이썬 Phase A~D(1,041줄)를 `AdaptiveMold.Core` 로 옮기고, **골든 픽스처와 값·플래그·폴백 가지가 일치**함을 증명한다.

**Architecture:** 파이썬 모듈 경계를 1:1로 유지한다 — `Grid` / `Optimization` / `Extension` / `Projection` + 계약을 담는 `MoldSolver` + 출력 묶음 `MoldResult`. 포팅 **전에** 파이썬 쪽을 먼저 손본다(진단 출력·런타임 메시지·중간 지문·T9 케이스). 그 변경이 계산을 건드리지 않았다는 증명은 **픽스처 `expected` 6배열이 불변**인 것이다.

**Tech Stack:** C# / `net7.0-windows` / RhinoCommon 8.0.23304.9001 / NUnit 3.14 (Rhino 안 NUnitLite) / Python (Rhino 안, 브리지 경유)

**Spec:** [`docs/superpowers/specs/2026-08-14-amv1-gha-yak-design.md`](../specs/2026-08-14-amv1-gha-yak-design.md)
**선행:** [M2a 계획](2026-08-14-amv1-gha-m2a.md) · [J-015 저널](../../journal/J-015-M2a-toolchain-and-packaging.md)

---

## 진행 상황 (2026-08-15)

| Task | 상태 |
|---|---|
| 1. 불변 검사 도구 | **✅ 완료** — `790f3be` |
| 2. Phase B·C 가지 진단 | **✅ 완료** — `7498f14` (전 케이스 `opt=full`/`ext=surface_extend`) |
| 3. 중간 산출물 지문 | **✅ 완료** — `52275c2` |
| 4. 런타임 메시지 정비 | **✅ 완료** — `c12fb14` |
| 5. T9 `ray-` 케이스 | **✅ 완료** — `3bce9e8` (진폭 1200, 커버리지 3/7→4/7) |
| **Phase 1 (파이썬 선반영) 끝** | **✅** — 픽스처 9개, 게이트 통과 |
| 6. `MoldResult` | ⬜ **여기부터** |
| 7~13 | ⬜ |

**재개 절차:**
1. worktree `C:\Users\leeja\Documents\dev\26_AdaptiveMold_amv1`, 브랜치 `feature/gha-port`
2. Rhino 8 실행 (`/netcore`) → 명령창에 **`mcpstart`** ← Task 2 끝의 픽스처 재생성에 필요
3. 기준이 없으면 먼저 만든다: `python adaptive_mold/tools/check_fixture_invariance.py --save`
   (`plugin/fixtures/_baseline/` 은 gitignore 라 체크아웃에 따라오지 않는다)
4. Task 2 Step 1부터

**현재 상태 확인용:**
```bash
python adaptive_mold/tools/check_fixture_invariance.py   # [PASS] 여야 한다
python adaptive_mold/tools/run_csharp_tests.py           # 26/26 (mcpstart 후)
```

> **Task 2 는 파이썬 4개 파일을 동시에 건드린다**(`optimization.py`·`extension.py`·
> `adaptive_mold_v1.py`·`dump_fixtures.py`). 중간에 끊으면 반쯤 배선된 상태가 되므로
> Step 1~8을 한 번에 갈 수 있을 때 시작할 것.

## Global Constraints

M2a 계획의 제약이 **전부 그대로 유효하다.** 아래는 그중 M2b 에서 특히 걸리는 것과, M2a 실측으로 새로 확정된 것이다.

- **성공 기준은 "똑같다"이지 "더 좋다"가 아니다.** 알고리즘을 개선하지 않는다. 파이썬이 이상하게 하는 것(부호 소실, `T5` 가 클램핑을 안 하는 것)도 **그대로 옮긴다.**
- **NuGet 은 `8.0.23304.9001` 고정.** 와일드카드 금지 — 컴포넌트가 조용히 사라진다(J-015 TRAP-03). `PackagingContractTests` 가 막는다.
- **`Core` 5대 규약** (스펙 §3.1): Grasshopper·`RhinoDoc`·`IGH_DataAccess` 미참조, RhinoCommon 기하는 UI 스레드에서만, **메시지는 던지지 않고 `MoldResult` 에 모아서 돌려준다.**
- **병렬화·비동기 금지.** `Task.Run`·`Parallel.For` 를 쓰지 않는다. 실측 근거: 파이썬 T8 이 25~49 ms.
- **인덱싱은 `idx = j * nx + i`.**
- **테스트는 Rhino 안에서 돈다.** `dotnet build` **성공을 눈으로 확인한 뒤** `python adaptive_mold/tools/run_csharp_tests.py`. 빌드가 실패하면 옛 DLL 로 통과가 나온다(도구가 낡음을 감지하지만 그 앞에 빌드를 볼 것).
- **`bin` 을 손으로 `LoadFrom` 하지 않는다.** 한 번 하면 그 Rhino 세션 내내 재빌드가 막힌다.
- **`mcpstart` 는 사람이 친다.** MCP 계층이 죽어 있으면 TCP 직결(`adaptive_mold/tools/rhino_cli.py`)로 우회한다. 포트 개방은 준비 신호가 아니다.
- **브리지는 IronPython 2.7이다.** 한글 `print` 금지(파일로 남기고 밖에서 읽을 것), `io.open` 은 `try/finally` 로 명시적 `close()`, **경로에 백슬래시를 쓰지 않는다**(heredoc 이 먹는다).
- **저널 번호는 `J-016`.** 새 저널 전에 `git ls-tree --name-only feature/unfold-v1 docs/journal/` 로 다른 브랜치를 확인할 것.

---

## File Structure

| 파일 | 책임 |
|---|---|
| `adaptive_mold/src/projection.py` | 수정 — 신규 경고(클램핑) |
| `adaptive_mold/src/optimization.py` | 수정 — `opt_branch` 진단, 메시지 재작성 |
| `adaptive_mold/src/extension.py` | 수정 — `ext_branch` 진단, 메시지 등급 조정 |
| `adaptive_mold/src/grid.py` | 수정 — spacing 나머지 Remark |
| `adaptive_mold/src/utils.py` | 수정 — 다중 face 경고 |
| `adaptive_mold/src/adaptive_mold_v1.py` | 수정 — 메시지 한국어화, compute Remark, 진단 배선 |
| `adaptive_mold/tools/dump_fixtures.py` | 수정 — `messages`·중간 지문·T9 케이스 |
| `plugin/fixtures/T*.json` | 재생성 (+`T9_deep_saddle.json`) |
| `plugin/AdaptiveMold.Core/MoldResult.cs` | 신규 — 출력 + 메시지 + 진단 |
| `plugin/AdaptiveMold.Core/Grid.cs` | 신규 ← `grid.py` |
| `plugin/AdaptiveMold.Core/Validation.cs` | 신규 ← `utils.py` 의 정규화·추출 |
| `plugin/AdaptiveMold.Core/Optimization.cs` | 신규 ← `optimization.py` |
| `plugin/AdaptiveMold.Core/Extension.cs` | 신규 ← `extension.py` |
| `plugin/AdaptiveMold.Core/Projection.cs` | 신규 ← `projection.py` |
| `plugin/AdaptiveMold.Core/MoldSolver.cs` | 신규 ← `run_adaptive_mold` 의 계약 |
| `plugin/AdaptiveMold.Tests/*Tests.cs` | 신규 — 단계별 대조 |
| `plugin/AdaptiveMold.Tests/TestSurfaceBuilder.cs` | 수정 — T9 의 `saddle` 추가 |
| `docs/journal/J-016-M2b-core-port.md` | 저널 |

**포팅 순서의 근거:** 픽스처가 직접 덮는 것부터 간다 — `Grid`(=`grid_pts`) → `Optimization`·`Extension`(중간 지문, Task 3에서 새로 만든다) → `Projection`(=배열 5개) → `MoldSolver`(계약). 마지막에 전 케이스 end-to-end.

> **스펙 §5.6 은 `A → D → B → C` 를 적었다. 이 계획은 `A → B → C → D` 로 바꾼다.**
> 그 순서는 "B·C 의 중간 산출물이 픽스처에 없으니 D 를 먼저 대조한다"는 전제였는데,
> **Task 3 이 그 전제를 없앤다** — 중간 지문이 생기면 B·C 를 각각 독립으로 판정할 수
> 있고, 그러면 파이프라인 순서대로 가는 것이 자연스럽다. D 를 먼저 하면 B·C 를
> 임시 구현으로 채워야 하는데 그 임시 구현이 틀리면 D 의 대조가 거짓으로 깨진다.

---

# Phase 1 — 파이썬 선반영

**이 구간의 공통 게이트:** 각 태스크 끝에서 픽스처를 재생성하고 **`expected` 6배열과 `summary` 가 불변**임을 확인한다. 하나라도 바뀌면 거기서 멈춘다 — 계산을 건드린 것이다.

재생성·검사는 매번 같으므로 먼저 도구로 만든다.

## Task 1: 불변 검사를 도구로 만든다 — ✅ 완료 (2026-08-15, `790f3be`)

**Files:**
- Create: `adaptive_mold/tools/check_fixture_invariance.py`

**Interfaces:**
- Consumes: `plugin/fixtures/T*.json`
- Produces: `python adaptive_mold/tools/check_fixture_invariance.py --save` (기준 저장) / 인자 없이 실행(대조). Task 2~4가 이것을 쓴다.

> **구현하면서 계획을 한 군데 고쳤다.** 원안은 `expected` 딕셔너리를 통째로
> 비교했는데, 그러면 **Task 2·3 이 하려는 일(진단 키 추가)마다 실패**하고
> 그때마다 "늘어난 키가 맞는지" 사람이 확인하는 단계가 붙는다.
> **키 단위 비교**로 바꾸고 늘어난 키는 `[ADDED]` 로 따로 보고하게 했다 —
> 계산이 바뀐 것과 진단이 늘어난 것을 도구가 구별한다. `--strict` 로 후자도 막는다.
> **따라서 아래 Task 2 Step 6·Task 3 Step 4·Task 4 Step 6 의 수동 확인 절차는
> 필요 없다.** 그냥 `check_fixture_invariance.py` 를 돌리고 `[ADDED]` 목록이
> 예상한 키뿐인지 보면 된다.
>
> 검사가 실제로 잡는 것을 네 시나리오로 확인했다: 계산값 변경 → FAIL,
> 입력 스펙 변경 → FAIL, 진단 키 추가 → PASS + `[ADDED]`, 같은 것에 `--strict` → FAIL.

- [x] **Step 1: 도구를 쓴다**

```python
#! /usr/bin/env python3
# -*- coding: utf-8 -*-
"""픽스처의 `expected`·`summary` 가 불변인지 확인한다.

파이썬 쪽을 고칠 때마다 "계산을 건드리지 않았다"를 증명하는 게이트다.
진단 출력·메시지·지문을 늘리는 것은 허용되지만, 계산 결과는 한 글자도
바뀌면 안 된다.

    python adaptive_mold/tools/check_fixture_invariance.py --save   # 기준 저장
    python adaptive_mold/tools/check_fixture_invariance.py          # 대조
"""

import argparse
import glob
import json
import os
import shutil
import sys

REPO = os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
FIXTURES = os.path.join(REPO, "plugin", "fixtures")
BASELINE = os.path.join(REPO, "plugin", "fixtures", "_baseline")

# 계산 결과. 이것들이 바뀌면 실패다.
GUARDED = ("expected", "summary")


def save():
    if os.path.isdir(BASELINE):
        shutil.rmtree(BASELINE)
    os.makedirs(BASELINE)
    n = 0
    for f in sorted(glob.glob(os.path.join(FIXTURES, "T*.json"))):
        shutil.copy(f, BASELINE)
        n += 1
    print("baseline saved: {} files".format(n))
    return 0


def check():
    if not os.path.isdir(BASELINE):
        print("[FAIL] no baseline. run with --save first")
        return 2

    bad = []
    for f in sorted(glob.glob(os.path.join(FIXTURES, "T*.json"))):
        name = os.path.basename(f)
        b = os.path.join(BASELINE, name)
        if not os.path.isfile(b):
            print("[NEW] {} (baseline에 없음 — 새 케이스라면 정상)".format(name))
            continue
        old = json.load(open(b, encoding="utf-8"))
        new = json.load(open(f, encoding="utf-8"))
        for key in GUARDED:
            if old.get(key) != new.get(key):
                bad.append("{} {}".format(name, key))
        # 입력 스펙도 바뀌면 안 된다 (지문·메타는 늘어도 된다)
        for key in ("surface", "base_plane", "params"):
            if old["input"].get(key) != new["input"].get(key):
                bad.append("{} input.{}".format(name, key))

    if bad:
        print("[FAIL] " + "; ".join(bad))
        return 1
    print("[PASS] expected/summary/input unchanged")
    return 0


def main():
    ap = argparse.ArgumentParser()
    ap.add_argument("--save", action="store_true")
    args = ap.parse_args()
    return save() if args.save else check()


if __name__ == "__main__":
    sys.exit(main())
```

- [x] **Step 2: 기준을 저장하고 검사가 통과하는지 본다**

Run:
```bash
python adaptive_mold/tools/check_fixture_invariance.py --save
python adaptive_mold/tools/check_fixture_invariance.py
```
Expected: `baseline saved: 8 files` → `[PASS] expected/summary/input unchanged`

- [x] **Step 3: 검사가 실제로 잡는지 확인한다 — 일부러 깨본다**

```bash
python -c "
import json,io
p='plugin/fixtures/T1_flat_parallel.json'
d=json.load(io.open(p,encoding='utf-8'))
d['expected']['pin_heights'][0] += 0.001
io.open(p,'w',encoding='utf-8').write(json.dumps(d,ensure_ascii=False,indent=2))
print('일부러 깨뜨림')
"
python adaptive_mold/tools/check_fixture_invariance.py
```
Expected: `[FAIL] T1_flat_parallel.json expected`

- [x] **Step 4: 원복하고 재확인**

```bash
git checkout plugin/fixtures/T1_flat_parallel.json
python adaptive_mold/tools/check_fixture_invariance.py
```
Expected: `[PASS]`

- [x] **Step 5: `_baseline` 을 `.gitignore` 에 넣고 커밋**

`.gitignore` 에 추가:
```gitignore
plugin/fixtures/_baseline/
```

```bash
git add .gitignore adaptive_mold/tools/check_fixture_invariance.py
git commit -m "test(fixtures): 불변 검사 도구 — 계산을 건드리지 않았다는 증명

파이썬 쪽을 고칠 때마다 expected/summary/입력스펙이 불변인지 본다.
진단·메시지·지문을 늘리는 것은 허용하고 계산 결과만 잠근다.
일부러 값을 흔들어 검사가 잡는 것을 확인했다."
```

---

## Task 2: 폴백 가지 진단을 Phase B·C 로 넓힌다 — ✅ 완료 (2026-08-15, `7498f14`)

**목적:** 지금 `branch_taken` 은 Phase D 만 덮는다. Phase B 에는 5갈래, Phase C 에는 3갈래가 있는데 **T1~T8 은 전부 정상 경로만 탄다.** 그 선택이 `opt_info`/`ext_method` 문자열로만 새어나가는데 둘 다 대조 대상이 아니다. C# 이 다른 가지를 타도 모른다.

**Files:**
- Modify: `adaptive_mold/src/optimization.py`
- Modify: `adaptive_mold/src/extension.py`
- Modify: `adaptive_mold/src/adaptive_mold_v1.py`
- Modify: `adaptive_mold/tools/dump_fixtures.py`

**Interfaces:**
- Produces: `AdaptiveMoldResult.opt_branch` (str), `.ext_branch` (str). 픽스처 `expected.opt_branch` · `expected.ext_branch`. Task 8·9의 C# `MoldResult.OptBranch`/`ExtBranch` 가 이 문자열과 **정확히 일치**해야 한다.

- [x] **Step 1: `optimization.py` 상단에 가지 상수를 넣는다**

```python
# ──────────────────────────────────────
# Phase B 가지 식별자 (골든 픽스처 대조용)
#
# projection.py 의 BRANCH_* 와 같은 목적이다. 어느 경로로 정렬했는지가
# 출력에 남지 않아, C# 포팅본이 다른 가지를 타도 숫자가 비슷하면 모른다.
# 값은 C# 포팅본과 문자열까지 일치해야 한다.
# ──────────────────────────────────────

OPT_INVALID_SURFACE = "invalid_surface"   # Brep 추출 실패
OPT_TRANSLATE_ONLY = "translate_only"     # 유효점 < 3, 평행이동만
OPT_NO_POINTS = "no_points"               # 유효점 0, 아무것도 안 함
OPT_FIT_NONE = "fit_none"                 # FitPlaneToPoints 가 None
OPT_FIT_FAILED = "fit_failed"             # fit 실패 → 평행이동만
OPT_DUP_FAILED = "dup_failed"             # DuplicateBrep 실패
OPT_FULL = "full"                         # 회전 + 평행이동 (정상 경로)
```

- [x] **Step 2: `optimize_surface` 가 가지를 함께 돌려주게 한다**

반환을 `(positioned, opt_info)` → `(positioned, opt_info, branch)` 로 바꾸고, 각 `return` 에 위 상수를 붙인다. **계산은 한 줄도 건드리지 않는다.**

```python
    brep = get_brep_from_input(target_srf)
    if brep is None:
        return (None, "optimization skipped: invalid surface", OPT_INVALID_SURFACE)
```
같은 식으로 7개 반환 지점 전부. 정상 경로(마지막)는 `OPT_FULL`.

- [x] **Step 3: `extension.py` 도 같게 한다**

```python
EXT_NO_SURFACE = "no_surface"       # get_surface_from_input 실패
EXT_SURFACE_EXTEND = "surface_extend"   # Surface.Extend 성공
EXT_TANGENT_FALLBACK = "tangent_fallback"  # 실패 → 접평면 외삽
```

`extend_surface` 반환을 `(extended, method)` → `(extended, method, branch)` 로.
**`method` 는 그대로 둔다** — Phase D 의 분기가 그 문자열을 쓰므로 바꾸면 계산이 바뀐다.

- [x] **Step 4: `adaptive_mold_v1.py` 에서 배선한다**

`AdaptiveMoldResult.__init__` 에 두 줄 추가:
```python
        self.opt_branch = ""
        self.ext_branch = ""
```

호출부를 3-튜플로 받게 고치고 `result.opt_branch = ...` / `result.ext_branch = ...` 로 채운다.

- [x] **Step 5: `dump_fixtures.py` 가 두 값을 덤프하게 한다**

`run_case` 반환 dict 에:
```python
        "opt_branch": r.opt_branch,
        "ext_branch": r.ext_branch,
```
`main()` 의 `expected` 에:
```python
                "opt_branch": first["opt_branch"],
                "ext_branch": first["ext_branch"],
```
`compare()` 의 검사 키 목록에도 두 개를 더한다 — **재현성 검사 대상이다.**

- [x] **Step 6: 재생성하고 불변을 확인한다**

Rhino 8 실행 → 명령창 `mcpstart` → 밖에서:
```bash
python adaptive_mold/tools/dump_via_bridge.py
python adaptive_mold/tools/check_fixture_invariance.py
```
Expected: `전 케이스 재현 확인` → `[PASS]`

> **주의:** `expected` 에 키가 **늘어난다**. 불변 검사는 `expected` 딕셔너리 전체를 비교하므로 **여기서는 실패한다.** 늘어난 키가 `opt_branch`·`ext_branch` 둘뿐이고 기존 6배열이 그대로인지 눈으로 확인한 뒤, 기준을 다시 저장한다:
> ```bash
> python -c "
> import json,glob,os
> for f in sorted(glob.glob('plugin/fixtures/T*.json')):
>     d=json.load(open(f,encoding='utf-8'))
>     b=json.load(open(os.path.join('plugin/fixtures/_baseline',os.path.basename(f)),encoding='utf-8'))
>     added=set(d['expected'])-set(b['expected'])
>     same=all(d['expected'][k]==b['expected'][k] for k in b['expected'])
>     print(os.path.basename(f), 'added=',sorted(added), 'old-keys-same=',same)
> "
> ```
> 전 케이스가 `added= ['ext_branch','opt_branch'] old-keys-same= True` 여야 한다. 그 뒤 `--save`.

- [x] **Step 7: 가지 값이 실제로 무엇인지 기록한다**

```bash
python -c "
import json,glob,os
for f in sorted(glob.glob('plugin/fixtures/T*.json')):
    d=json.load(open(f,encoding='utf-8'))
    print('%-26s opt=%-14s ext=%s' % (os.path.basename(f), d['expected']['opt_branch'], d['expected']['ext_branch']))
"
```

**전 케이스가 `opt=full` 이고 `ext` 가 한 값뿐이면 그것이 커버리지 한계다** — 저널에 그대로 적는다. Phase D 의 3/7 과 같은 성질의 사실이다.

- [x] **Step 8: 커밋**

```bash
python adaptive_mold/tools/check_fixture_invariance.py --save
git add adaptive_mold/src/ adaptive_mold/tools/ plugin/fixtures/
git commit -m "feat(diag): Phase B·C 가지 진단 — branch_taken 이 D 만 덮고 있었다

Phase B 5갈래, Phase C 3갈래의 선택이 opt_info/ext_method 문자열로만
새어나가고 대조 대상이 아니었다. C# 이 다른 가지를 타도 숫자가
비슷하면 모른다.

opt_branch/ext_branch 를 결과 객체와 픽스처 expected 에 추가했다.
계산은 한 줄도 안 건드렸고 기존 6배열 불변을 확인했다."
```

---

## Task 3: 중간 산출물 지문 — Phase B·C 를 직접 대조 가능하게 만든다 — ✅ 완료 (2026-08-15, `52275c2`)

**목적:** 스펙 §5.5 는 Phase B·C 를 "중간 산출물이 픽스처에 없으므로 임시 덤프로 확인"한다고 적었다. **그것을 정본으로 승격한다.** M2a 에서 입력 지문이 통했으므로 같은 방식이 중간 산출물에도 통한다. 이러면 포팅에서 가장 약한 고리가 사라진다.

> **스펙에서 벗어나는 결정이다.** §5.5 는 "개발 중 수단이지 회귀 테스트가 아니다"라고 했다. 뒤집는 근거: 비용이 지문 함수 재사용 한 번이고, 얻는 것은 **B·C 를 각각 독립적으로 판정할 수 있는 능력**이다. 이것이 없으면 Phase D 대조가 깨졌을 때 B·C·D 중 어디가 문제인지 이분할 수 없다.

**Files:**
- Modify: `adaptive_mold/tools/dump_fixtures.py`
- Modify: `plugin/fixtures/T*.json` (재생성)

**Interfaces:**
- Produces: `expected.positioned_fingerprint`, `expected.extended_fingerprint` (형식은 M2a 의 `geometry_fingerprint` 와 동일). Task 8·9 가 대조한다.

- [x] **Step 1: `run_case` 가 중간 산출물의 지문을 재게 한다**

`run_adaptive_mold` 는 `result.positioned_srf`·`result.extended_srf` 를 이미 들고 있다. 반환 dict 에 두 줄 추가:

```python
        "positioned_fingerprint": (geometry_fingerprint(r.positioned_srf)
                                   if r.positioned_srf is not None else None),
        "extended_fingerprint": (geometry_fingerprint(r.extended_srf)
                                 if r.extended_srf is not None else None),
```

- [x] **Step 2: `expected` 에 넣고 `compare()` 에도 넣는다**

```python
                "positioned_fingerprint": first["positioned_fingerprint"],
                "extended_fingerprint": first["extended_fingerprint"],
```

`compare()` 의 키 목록에도 추가한다 — 지문이 실행마다 흔들리면 정답지가 될 수 없다.

- [x] **Step 3: 재생성**

Rhino + `mcpstart` 후:
```bash
python adaptive_mold/tools/dump_via_bridge.py
```
Expected: `전 케이스 재현 확인`

- [x] **Step 4: 기존 6배열 불변을 확인하고 기준을 갱신한다**

```bash
python -c "
import json,glob,os
ok=True
for f in sorted(glob.glob('plugin/fixtures/T*.json')):
    d=json.load(open(f,encoding='utf-8'))
    b=json.load(open(os.path.join('plugin/fixtures/_baseline',os.path.basename(f)),encoding='utf-8'))
    same=all(d['expected'][k]==b['expected'][k] for k in b['expected'])
    ok = ok and same
    print('%-26s old-keys-same=%s' % (os.path.basename(f), same))
print('ALL OK' if ok else 'FAIL')
"
python adaptive_mold/tools/check_fixture_invariance.py --save
```
Expected: 전 케이스 `old-keys-same=True`, `ALL OK`

- [x] **Step 5: 지문이 케이스마다 실제로 다른지 본다 — 같으면 쓸모가 없다**

```bash
python -c "
import json,glob,os
for f in sorted(glob.glob('plugin/fixtures/T*.json')):
    d=json.load(open(f,encoding='utf-8'))['expected']
    p=d.get('positioned_fingerprint'); e=d.get('extended_fingerprint')
    print('%-26s pos_bbox=%s ext_bbox=%s' % (os.path.basename(f),
          p and p['bbox_max'], e and e['bbox_max']))
"
```

**T1~T8 의 `positioned` bbox 가 전부 같다면 지문이 Phase B 를 구별하지 못한다는 뜻이다.** 그 경우 저널에 적고, 대조 항목에 `area` 를 더 신뢰하거나 샘플 점을 추가할지 결정한다.

- [x] **Step 6: 커밋**

```bash
git add adaptive_mold/tools/dump_fixtures.py plugin/fixtures/
git commit -m "feat(fixtures): 중간 산출물 지문 — Phase B·C 를 직접 대조한다

스펙 §5.5 는 B·C 를 '임시 덤프로 확인'한다고 했지만 정본으로 올린다.
M2a 에서 입력 지문이 통했으므로 같은 방식이 중간 산출물에도 통한다.
없으면 Phase D 대조가 깨졌을 때 B·C·D 중 어디가 문제인지 이분할 수
없다. 비용은 지문 함수 재사용 한 번이다.

기존 expected 6배열 불변 확인."
```

---

## Task 4: 런타임 메시지 정비 + 신규 4개 — ✅ 완료 (2026-08-15, `c12fb14`)

**목적:** 지금 A~D 에 **클램핑 경고가 한 줄도 없다.** 픽스처 실측으로 T3 에서 핀의 56%(20/36)가 목표를 재현 못 하는데 캔버스는 초록이다. 그리고 F1 이 죽어 있어(J-015 FACT-09) **런타임 메시지가 유일한 push 채널**이다.

**Files:**
- Modify: `adaptive_mold/src/utils.py`, `grid.py`, `extension.py`, `optimization.py`, `adaptive_mold_v1.py`
- Modify: `adaptive_mold/tools/dump_fixtures.py`

**Interfaces:**
- Produces: 픽스처 `expected.messages` = `[[level, text], …]` (level 은 `"Error"`/`"Warning"`/`"Remark"`). Task 5의 C# `MoldMessage` 와 **등급·문안이 정확히 일치**해야 한다.

- [x] **Step 1: 메시지를 수집할 수 있게 가짜 component 를 만든다**

`dump_fixtures.py` 에 추가:

```python
class _MessageCollector(object):
    """`utils.add_*` 가 부르는 GhPython 컴포넌트 흉내.

    픽스처는 지금까지 component=None 으로 뽑혀 메시지가 한 줄도 없었다.
    '검증 가능하다'와 '정답지가 있다'는 다르다.
    """

    def __init__(self):
        self.messages = []

    def AddRuntimeMessage(self, level, text):
        self.messages.append([str(level), text])
```

`utils.add_warning` 등이 실제로 어떤 형태로 부르는지 먼저 확인하고 맞춘다:
```bash
sed -n '18,45p' adaptive_mold/src/utils.py
```

- [x] **Step 2: `run_case` 가 수집기를 넘기게 한다**

```python
    collector = _MessageCollector()
    r = run_adaptive_mold(..., component=collector)
```
반환 dict 에 `"messages": collector.messages`.

- [x] **Step 3: 지금 나오는 메시지를 먼저 본다 — 고치기 전에**

재생성 후:
```bash
python -c "
import json,glob,os
for f in sorted(glob.glob('plugin/fixtures/T*.json')):
    d=json.load(open(f,encoding='utf-8'))
    print(os.path.basename(f))
    for lv,tx in d['expected'].get('messages',[]):
        print('   [%s] %s' % (lv, tx))
"
```

**이 목록이 다음 단계의 작업 대상이다.** 예상: T3 에 클램핑 경고가 없고, 영어와 한국어가 섞여 있다.

- [x] **Step 4: 신규 경고 4개를 넣는다**

**(a) 클램핑 — `adaptive_mold_v1.py`, Phase D 직후**

```python
    n_clamped = sum(1 for c in clamp_flags if c)
    if n_clamped > 0:
        add_warning(component,
                    u"핀 {}/{} 개가 행정 한계에 걸렸다. 그 지점은 목표 곡면을 "
                    u"재현하지 못한다. clamp_flags 로 위치를 확인하고, "
                    u"max_height 를 늘리거나 곡면을 완만하게 할 것."
                    .format(n_clamped, total_pins))
```

**(b) 다중 face — `utils.py::get_surface_from_input` 은 component 를 안 받는다.** 호출부인 `extension.py::extend_surface` 에서 검사한다:

```python
    brep_in = get_brep_from_input(positioned_srf)
    if brep_in is not None and brep_in.Faces.Count > 1:
        add_warning(component,
                    u"target_srf 의 face 가 {} 개다. 곡면 확장(Phase C)은 첫 face "
                    u"만 쓰므로 결과가 나머지 face 를 반영하지 않는다. 단일 face 로 "
                    u"합치거나 face 마다 따로 돌릴 것.".format(brep_in.Faces.Count))
```

**(c) spacing 나머지 — `adaptive_mold_v1.py`, Phase A 직후**

```python
    rem_x = width - (nx - 1) * spacing
    rem_y = length - (ny - 1) * spacing
    if rem_x > 1e-9 or rem_y > 1e-9:
        add_remark(component,
                   u"spacing {:g} 이 몰드 크기를 나누지 못한다. 핀은 X 방향 {:g}mm, "
                   u"Y 방향 {:g}mm 까지만 덮는다 (나머지 {:g}/{:g}mm 는 비어 있다)."
                   .format(spacing, (nx - 1) * spacing, (ny - 1) * spacing, rem_x, rem_y))
```

**(d) compute 꺼짐 — `adaptive_mold_v1.py` 맨 앞**

```python
    if not compute:
        result.info = u"Compute is disabled. Set compute=True to run."
        add_remark(component, u"compute 가 꺼져 있다. 계산하려면 Boolean Toggle 을 True 로.")
        return result
```

- [x] **Step 5: 기존 메시지를 한국어로 통일하고 규약에 맞춘다**

규약: **`[무엇이] · [결과가 어떻게 됐는지] · [다음에 무엇을]`**. 파라미터명·수치·API 이름은 원문 유지.

| 파일:줄 | 현재 | 바꿀 문안 |
|---|---|---|
| `adaptive_mold_v1.py:114` | `min_height (0) must be < max_height (400)` | `min_height ({}) 가 max_height ({}) 보다 크거나 같다. 계산하지 않았다. 두 값을 확인할 것.` |
| `:119` | `target_srf is required.` | `target_srf 가 비어 있다. Surface 또는 Brep 을 물릴 것.` |
| `:124` | `target_srf is invalid.` | `target_srf 에서 Brep 을 얻지 못했다. Surface 또는 Brep 을 물릴 것 (Mesh·Curve·Point 는 받지 않는다).` |
| `:144` | `Surface optimization failed.` | `Phase B 정렬이 실패했다 (DuplicateBrep 실패). 곡면이 유효한지 확인할 것.` |
| `:193` | `AdaptiveMold v1 computed successfully.` | `AdaptiveMold v1 계산 완료. 핀 {}개.` (개수를 넣어 빈 문장을 없앤다) |
| `optimization.py:51` | `Not enough in-bounds points for optimization (0)` | `곡면 위에 놓인 격자점이 {}개다 (정렬에 최소 3개 필요). Phase B 정렬을 건너뛰었고 모든 핀 높이가 외삽값이다. base_plane 의 원점은 몰드의 모서리다 — 곡면이 X [0, {:g}] · Y [0, {:g}] 밖에 있는지 확인할 것.` |
| `extension.py:40` | `Surface extension: could not extract surface` | `positioned_srf 에서 Surface 를 얻지 못해 곡면 확장을 건너뛰었다. 몰드 밖 핀은 전부 접평면 외삽값이 된다.` |
| `extension.py:63` | Warning `Surface.Extend() failed, using tangent fallback` | **Remark 로 강등** + `곡면 확장에 Surface.Extend 를 쓰지 못해 접평면 외삽으로 대체했다. 몰드 안쪽(extension_flags=False) 핀은 영향이 없다.` |
| `grid.py:28` | (이미 한국어) | 그대로 |
| `optimization.py:141` | (숫자 있음, 양호) | 한국어화만 |

**등급 변경은 `extension.py:63` 하나뿐이다.** 흔하게 뜨는 Warning 은 사용자에게 경고 무시를 학습시킨다.

- [x] **Step 6: 재생성하고 불변을 확인한다 — 여기가 이 태스크의 합격 조건**

```bash
python adaptive_mold/tools/dump_via_bridge.py
python -c "
import json,glob,os
ok=True
for f in sorted(glob.glob('plugin/fixtures/T*.json')):
    d=json.load(open(f,encoding='utf-8'))
    b=json.load(open(os.path.join('plugin/fixtures/_baseline',os.path.basename(f)),encoding='utf-8'))
    same=all(d['expected'][k]==b['expected'][k] for k in b['expected'] if k!='messages')
    ok = ok and same
    print('%-26s calc-same=%s' % (os.path.basename(f), same))
print('ALL OK' if ok else 'FAIL')
"
```
Expected: 전 케이스 `calc-same=True`

**바뀌었다면 거기서 멈춘다.** 메시지 추가가 계산을 건드렸다는 뜻이고, 가장 의심스러운 곳은 Step 4(b)의 `Faces.Count` 접근이 Brep 을 만졌을 가능성이다.

- [x] **Step 7: 클램핑 경고가 실제로 T3 에 뜨는지 확인한다**

```bash
python -c "
import json
d=json.load(open('plugin/fixtures/T3_hemisphere.json',encoding='utf-8'))
for lv,tx in d['expected']['messages']:
    print('[%s] %s' % (lv,tx))
"
```
Expected: `[Warning] 핀 20/36 개가 행정 한계에 걸렸다…` 가 있어야 한다.

- [x] **Step 8: 커밋**

```bash
python adaptive_mold/tools/check_fixture_invariance.py --save
git add adaptive_mold/ plugin/fixtures/
git commit -m "feat(msg): 런타임 메시지 정비 + 신규 4개 — 클램핑이 조용했다

A~D 에 클램핑 경고가 한 줄도 없었다. T3(반구)에서 핀의 56%가 행정
한계에 걸리는데 캔버스는 초록이었다. F1 이 죽어 있어(J-015 FACT-09)
런타임 메시지가 유일한 push 채널이다.

신규 4개: 클램핑(W) / 다중 face(W) / spacing 나머지(R) / compute 꺼짐(R).
기존 8개를 [무엇이·결과·다음에 무엇을] 규약으로 재작성하고 한국어로
통일했다. 등급 변경은 Surface.Extend 실패 하나(W->R) — 흔하게 뜨는
경고는 무시를 학습시킨다.

픽스처에 messages 를 정답지로 추가했다. 지금까지 component=None 으로
뽑혀 한 줄도 없었다 — '검증 가능하다'와 '정답지가 있다'는 다르다.
계산 결과 불변 확인."
```

---

## Task 5: T9 — `ray-` 가지를 태우는 케이스 — ✅ 완료 (2026-08-15, `3bce9e8`)

**목적:** 완료조건 4("폴백 가지가 핀별로 일치")가 지금은 **3/7 만 보고 있다**(J-015 이전에 5/7로 잘못 적혀 있었다). 미커버는 `ray-/panel`·`ray-/ext`·`closest`·`default`. 최소한 `ray-` 하나는 되살린다.

**Files:**
- Modify: `adaptive_mold/tools/dump_fixtures.py` (`saddle` kind + T9 케이스)
- Modify: `plugin/AdaptiveMold.Tests/TestSurfaceBuilder.cs` (같은 kind)
- Create: `plugin/fixtures/T9_deep_saddle.json`

**Interfaces:**
- Produces: 9번째 픽스처. 이후 모든 대조가 9개를 돈다.

- [x] **Step 1: `saddle` 곡면 생성기를 파이썬에 넣는다**

`ray-` 를 태우려면 **Phase B 가 평균을 stroke 중앙(200)으로 옮긴 뒤에도 가장자리가 그리드 아래로 내려가는** 진폭 큰 곡면이 필요하다.

```python
def _make_saddle(amplitude=600.0, size=2000.0, z_base=200.0):
    """쌍곡포물면. 진폭이 커서 정렬 후에도 가장자리가 그리드 아래로 내려간다.

    ray- 가지(역법선 레이캐스트)를 태우기 위한 케이스다. T1~T8 은 그 가지를
    한 번도 타지 않아 완료조건 4가 절반 빈 검사였다.
    """
    half = size / 2.0
    pts = []
    n = 5
    for i in range(n):
        for j in range(n):
            u = -half + size * i / (n - 1.0)
            v = -half + size * j / (n - 1.0)
            z = z_base + amplitude * (u * v) / (half * half)
            pts.append(rg.Point3d(500.0 + u, 500.0 + v, z))
    srf = rg.NurbsSurface.CreateThroughPoints(pts, n, n, 3, 3, False, False)
    return srf
```

`build_surface` 에 분기를 더한다:
```python
    if kind == "saddle":
        return _make_saddle(spec["amplitude"], spec["size"], spec["z_base"])
```

- [x] **Step 2: 케이스를 `CASES` 에 추가한다**

```python
    {
        "case": "T9_deep_saddle",
        "note": "deep hyperbolic paraboloid - edges dip below grid after Phase B, "
                "exercises ray- branch which T1~T8 never touch",
        "surface": {"kind": "saddle", "amplitude": 600.0, "size": 2000.0, "z_base": 200.0},
        "base_plane": None,
        "params": {},
    },
```

- [x] **Step 3: 재생성하고 `ray-` 가 실제로 켜지는지 본다 — 이 태스크의 판정**

```bash
python adaptive_mold/tools/dump_via_bridge.py
python -c "
import json,glob,os,collections
u=collections.Counter()
for f in sorted(glob.glob('plugin/fixtures/T*.json')):
    d=json.load(open(f,encoding='utf-8'))
    bc=d['summary']['branch_counts']
    print('%-26s %s' % (os.path.basename(f), bc))
    u.update(bc)
print()
print('합집합:', dict(u))
print('커버리지: %d/7' % len(u))
"
```

**`ray-/panel` 또는 `ray-/ext` 가 합집합에 나타나야 한다.**

- [x] **Step 4: 안 켜지면 진폭을 키워 한 번 더 — 그래도 안 되면 정직하게 낮춘다**

`amplitude` 를 600 → 1200 으로 올려 Step 3을 반복한다. 두 번 해서 안 되면 **케이스를 되돌리고**(`git checkout`) 스펙 §8 완료조건 4를 이렇게 고친다:

> 4. 폴백 가지 선택이 파이썬과 핀별로 일치한다 — **커버된 3가지(`ray+/panel`·`ray+/ext`·`tangent`)에 한해.** `ray-`·`closest`·`default` 는 T1~T9 이 태우지 못해 **검증되지 않는다.**

**되지도 않는 조건을 걸어두는 것이 더 나쁘다.** 저널에 시도한 진폭과 결과를 적는다.

- [x] **Step 5: C# `TestSurfaceBuilder` 에 같은 분기를 넣는다**

```csharp
                case "saddle":
                    return MakeSaddle(
                        spec.GetProperty("amplitude").GetDouble(),
                        spec.GetProperty("size").GetDouble(),
                        spec.GetProperty("z_base").GetDouble());
```

```csharp
        // _make_saddle — 제어점 격자를 그대로 옮긴다. 순서가 다르면 곡면이 달라진다.
        static Brep MakeSaddle(double amplitude, double size, double zBase)
        {
            double half = size / 2.0;
            const int n = 5;
            var pts = new List<Point3d>();
            for (int i = 0; i < n; i++)
                for (int j = 0; j < n; j++)
                {
                    double u = -half + size * i / (n - 1.0);
                    double v = -half + size * j / (n - 1.0);
                    double z = zBase + amplitude * (u * v) / (half * half);
                    pts.Add(new Point3d(500.0 + u, 500.0 + v, z));
                }
            var srf = NurbsSurface.CreateThroughPoints(pts, n, n, 3, 3, false, false);
            return srf.ToBrep();
        }
```

`using System.Collections.Generic;` 를 파일 상단에 더한다.

- [x] **Step 6: 지문 테스트로 C# 이 같은 안장면을 만드는지 확인한다**

```bash
cd plugin && dotnet build LjksAdaptiveMold.sln && cd ..
python adaptive_mold/tools/run_csharp_tests.py
```
Expected: `total=28 passed=28` (지문 16 → 18로 늘어난다)

**어긋나면 파이썬을 정본으로 맞춘다.** 제어점 순서(`i` 바깥 / `j` 안쪽)가 가장 흔한 원인이다.

- [x] **Step 7: 커밋**

```bash
python adaptive_mold/tools/check_fixture_invariance.py --save
git add adaptive_mold/tools/ plugin/
git commit -m "feat(fixtures): T9 깊은 안장면 — ray- 가지를 되살린다

완료조건 4가 3/7 만 보고 있었다. 미커버는 ray-/panel·ray-/ext·
closest·default 넷. 진폭 큰 쌍곡포물면이면 Phase B 가 평균을 stroke
중앙으로 옮긴 뒤에도 가장자리가 그리드 아래로 내려가 ray- 를 탄다.

C# TestSurfaceBuilder 에 같은 분기를 넣고 지문 일치를 확인했다."
```

---

# Phase 2 — Core 포팅

**이 구간의 공통 절차:** 테스트를 **먼저** 쓰고 실패를 확인한 뒤 구현한다. 매번 `dotnet build` 성공을 눈으로 본 뒤 `python adaptive_mold/tools/run_csharp_tests.py`.

**픽스처를 도는 테스트 파일의 골격.** 아래 태스크들에서 메서드만 보여주는 경우 이 골격 안에 넣는다 (`<Name>` 을 `Optimization`/`Extension`/`Projection`/`MoldSolver` 로):

```csharp
using System.Collections.Generic;
using System.IO;
using System.Text.Json;
using NUnit.Framework;
using AdaptiveMold.Core;
using Rhino.Geometry;

namespace AdaptiveMold.Tests
{
    public class <Name>Tests
    {
        static string FixtureDir =>
            Path.Combine(TestContext.CurrentContext.TestDirectory, "fixtures");

        public static IEnumerable<string> FixtureFiles()
        {
            foreach (var p in Directory.GetFiles(FixtureDir, "T*.json"))
                yield return p;
        }

        // ── 태스크가 지정하는 테스트 메서드들이 여기 들어간다 ──
    }
}
```

## Task 6: `MoldResult` · `MoldMessage` — 출력 그릇

**Files:**
- Create: `plugin/AdaptiveMold.Core/MoldResult.cs`
- Create: `plugin/AdaptiveMold.Tests/MoldResultTests.cs`

**Interfaces:**
- Produces: `MoldResult`(필드는 아래), `MoldMessage`, `MoldMessageLevel`. **이후 모든 태스크가 이 타입을 쓴다.**

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```csharp
using NUnit.Framework;
using AdaptiveMold.Core;

namespace AdaptiveMold.Tests
{
    public class MoldResultTests
    {
        [Test]
        public void New_result_is_empty_but_not_null()
        {
            var r = new MoldResult();

            Assert.That(r.PinHeights, Is.Not.Null.And.Empty);
            Assert.That(r.ClampFlags, Is.Not.Null.And.Empty);
            Assert.That(r.ExtensionFlags, Is.Not.Null.And.Empty);
            Assert.That(r.GridPts, Is.Not.Null.And.Empty);
            Assert.That(r.PinTops, Is.Not.Null.And.Empty);
            Assert.That(r.BranchTaken, Is.Not.Null.And.Empty);
            Assert.That(r.Messages, Is.Not.Null.And.Empty);
            Assert.That(r.Info, Is.EqualTo(string.Empty));
        }

        [Test]
        public void Messages_keep_level_and_order()
        {
            var r = new MoldResult();
            r.AddRemark("첫 번째");
            r.AddWarning("두 번째");
            r.AddError("세 번째");

            Assert.That(r.Messages.Count, Is.EqualTo(3));
            Assert.That(r.Messages[0].Level, Is.EqualTo(MoldMessageLevel.Remark));
            Assert.That(r.Messages[1].Level, Is.EqualTo(MoldMessageLevel.Warning));
            Assert.That(r.Messages[2].Level, Is.EqualTo(MoldMessageLevel.Error));
            Assert.That(r.Messages[2].Text, Is.EqualTo("세 번째"));
        }
    }
}
```

- [ ] **Step 2: 실패 확인**

Run: `cd plugin && dotnet build AdaptiveMold.Tests/AdaptiveMold.Tests.csproj`
Expected: `MoldResult` 형식이 없다는 컴파일 오류

- [ ] **Step 3: 구현**

```csharp
using System.Collections.Generic;
using Rhino.Geometry;

namespace AdaptiveMold.Core
{
    public enum MoldMessageLevel { Remark, Warning, Error }

    public class MoldMessage
    {
        public MoldMessageLevel Level;
        public string Text;

        public MoldMessage(MoldMessageLevel level, string text)
        {
            Level = level;
            Text = text;
        }
    }

    /// <summary>
    /// Phase A~D 의 출력 묶음.
    ///
    /// 파이썬 <c>AdaptiveMoldResult</c> 와 **1:1이 아니다** — 후자는
    /// positioned_srf·extended_srf·housings·rods·tops 를 더 들고 있다.
    /// 여기서는 **중간 Brep 을 붙들지 않는다**(설계 §3.4). 필요 없는 것을
    /// 들고 있을 이유가 없고, GH 가 캔버스에 유지하는 것과 겹치면 낭비다.
    ///
    /// 메시지는 **던지지 않고 모아서 돌려준다**(설계 §3.1 규약 5).
    /// Core 가 GH 를 모르므로 어댑터가 이것을 런타임 메시지로 번역한다.
    /// </summary>
    public class MoldResult
    {
        public List<double> PinHeights = new List<double>();
        public List<bool> ClampFlags = new List<bool>();
        public List<bool> ExtensionFlags = new List<bool>();
        public List<Point3d> GridPts = new List<Point3d>();
        public List<Point3d> PinTops = new List<Point3d>();

        /// <summary>핀별로 탄 폴백 가지 (<see cref="Projection"/> 의 Branch* 상수).</summary>
        public List<string> BranchTaken = new List<string>();

        /// <summary>Phase B·C 가 탄 가지. 파이썬 opt_branch/ext_branch 와 문자열까지 같다.</summary>
        public string OptBranch = string.Empty;
        public string ExtBranch = string.Empty;

        public int Nx;
        public int Ny;

        public string Info = string.Empty;

        public List<MoldMessage> Messages = new List<MoldMessage>();

        public void AddRemark(string text) => Messages.Add(new MoldMessage(MoldMessageLevel.Remark, text));
        public void AddWarning(string text) => Messages.Add(new MoldMessage(MoldMessageLevel.Warning, text));
        public void AddError(string text) => Messages.Add(new MoldMessage(MoldMessageLevel.Error, text));
    }
}
```

- [ ] **Step 4: 통과 확인 후 커밋**

```bash
cd plugin && dotnet build LjksAdaptiveMold.sln && cd ..
python adaptive_mold/tools/run_csharp_tests.py
git add plugin/
git commit -m "feat(core): MoldResult — 중간 Brep 을 붙들지 않고 메시지를 모은다"
```

---

## Task 7: `Grid` — 픽스처가 직접 덮는 첫 모듈

**Files:**
- Create: `plugin/AdaptiveMold.Core/Grid.cs`
- Create: `plugin/AdaptiveMold.Tests/GridTests.cs`

**Interfaces:**
- Consumes: 없음
- Produces: `Grid.ComputeCounts(w,l,s) -> (int nx, int ny)`, `Grid.Build(plane,w,l,s) -> (List<Point3d> pts, int nx, int ny)`, `Grid.FlatIndex(i,j,nx)`, `Grid.Indices(idx,nx)`, `Grid.Center(plane,w,l)`. Task 10·11이 쓴다.

- [ ] **Step 1: 픽스처 대조 테스트를 먼저 쓴다**

```csharp
using System.Collections.Generic;
using System.IO;
using System.Text.Json;
using NUnit.Framework;
using AdaptiveMold.Core;
using Rhino.Geometry;

namespace AdaptiveMold.Tests
{
    public class GridTests
    {
        static string FixtureDir =>
            Path.Combine(TestContext.CurrentContext.TestDirectory, "fixtures");

        public static IEnumerable<string> FixtureFiles()
        {
            foreach (var p in Directory.GetFiles(FixtureDir, "T*.json"))
                yield return p;
        }

        [TestCaseSource(nameof(FixtureFiles))]
        public void Grid_points_match_python(string path)
        {
            using var doc = JsonDocument.Parse(File.ReadAllText(path));
            var input = doc.RootElement.GetProperty("input");
            var prm = input.GetProperty("params");

            var planeOrNull = TestSurfaceBuilder.BuildPlane(input.GetProperty("base_plane"));
            var plane = planeOrNull ?? Plane.WorldXY;

            var (pts, nx, ny) = Grid.Build(plane,
                prm.GetProperty("width").GetDouble(),
                prm.GetProperty("length").GetDouble(),
                prm.GetProperty("spacing").GetDouble());

            var expected = doc.RootElement.GetProperty("expected").GetProperty("grid_pts");

            Assert.That(pts.Count, Is.EqualTo(expected.GetArrayLength()), "핀 개수");
            Assert.That(nx * ny, Is.EqualTo(pts.Count), "nx*ny 와 개수가 어긋난다");

            for (int k = 0; k < pts.Count; k++)
            {
                var e = expected[k];
                Assert.That(pts[k].X, Is.EqualTo(e[0].GetDouble()).Within(1e-6), $"pt[{k}].X");
                Assert.That(pts[k].Y, Is.EqualTo(e[1].GetDouble()).Within(1e-6), $"pt[{k}].Y");
                Assert.That(pts[k].Z, Is.EqualTo(e[2].GetDouble()).Within(1e-6), $"pt[{k}].Z");
            }
        }

        [Test]
        public void Counts_clamp_to_two()
        {
            // grid.py:35-38 — nx<2 면 2로 올린다. 하류가 이 클램프를 빠뜨리고
            // int(width // spacing) + 1 로 재유도하고 있었다(AMv1_Inspect.py:322).
            var (nx, ny) = Grid.ComputeCounts(100.0, 100.0, 1000.0);
            Assert.That(nx, Is.EqualTo(2));
            Assert.That(ny, Is.EqualTo(2));
        }

        [Test]
        public void Invalid_spacing_throws()
        {
            var ex = Assert.Throws<System.ArgumentException>(
                () => Grid.ComputeCounts(1000.0, 1000.0, 0.0));
            Assert.That(ex.Message, Does.Contain("spacing"));
        }

        [Test]
        public void Index_roundtrip()
        {
            // idx = j * nx + i. 여기서 순서가 바뀌면 픽스처 대조가 전부 무의미해진다.
            const int nx = 6;
            for (int j = 0; j < 4; j++)
                for (int i = 0; i < nx; i++)
                {
                    int idx = Grid.FlatIndex(i, j, nx);
                    var (bi, bj) = Grid.Indices(idx, nx);
                    Assert.That((bi, bj), Is.EqualTo((i, j)));
                }
        }
    }
}
```

- [ ] **Step 2: 실패 확인**

Run: `cd plugin && dotnet build AdaptiveMold.Tests/AdaptiveMold.Tests.csproj`
Expected: `Grid` 형식이 없다는 컴파일 오류

- [ ] **Step 3: 구현**

```csharp
using System;
using System.Collections.Generic;
using Rhino.Geometry;

namespace AdaptiveMold.Core
{
    /// <summary>
    /// Phase A — 베이스 그리드 생성. <c>grid.py</c> 의 1:1 포팅.
    ///
    /// 인덱싱 규약 <c>idx = j * nx + i</c> (row-major, j=Y, i=X).
    /// 그리드 원점은 <c>basePlane.Origin</c> 이며 +X·+Y 로 뻗는다 —
    /// **몰드의 중심이 아니라 모서리다.**
    /// </summary>
    public static class Grid
    {
        public static (int nx, int ny) ComputeCounts(double width, double length, double spacing)
        {
            if (spacing <= 0)
                throw new ArgumentException($"spacing은 0보다 커야 합니다. 현재: {spacing}");
            if (width <= 0 || length <= 0)
                throw new ArgumentException("width와 length는 0보다 커야 합니다.");

            int nx = (int)Math.Floor(width / spacing) + 1;
            int ny = (int)Math.Floor(length / spacing) + 1;

            if (nx < 2) nx = 2;
            if (ny < 2) ny = 2;

            return (nx, ny);
        }

        public static (List<Point3d> pts, int nx, int ny) Build(
            Plane basePlane, double width, double length, double spacing)
        {
            var (nx, ny) = ComputeCounts(width, length, spacing);

            var pts = new List<Point3d>(nx * ny);
            for (int j = 0; j < ny; j++)
                for (int i = 0; i < nx; i++)
                    pts.Add(basePlane.PointAt(i * spacing, j * spacing, 0));

            return (pts, nx, ny);
        }

        public static (int i, int j) Indices(int index, int nx) => (index % nx, index / nx);

        public static int FlatIndex(int i, int j, int nx) => j * nx + i;

        public static Point3d Center(Plane basePlane, double width, double length) =>
            basePlane.PointAt(width / 2.0, length / 2.0, 0);
    }
}
```

> **파이썬은 `ValueError` 를 던지고 C# 은 `ArgumentException` 이다.** 메시지 문자열은 같게 유지한다 — `adaptive_mold_v1.py:131` 이 그 문자열을 그대로 Error 메시지로 올리므로, 문안이 달라지면 메시지 대조가 깨진다.

- [ ] **Step 4: 통과 확인 후 커밋**

```bash
cd plugin && dotnet build LjksAdaptiveMold.sln && cd ..
python adaptive_mold/tools/run_csharp_tests.py
git add plugin/
git commit -m "feat(core): Grid — 픽스처가 직접 덮는 첫 모듈

grid_pts 9케이스 전부 1e-6 이내 일치. nx<2 클램프도 테스트로 고정했다 —
하류가 int(width // spacing) + 1 로 재유도하며 이 클램프를 빠뜨리고 있다."
```

---

## Task 8: `Validation` — 입력 정규화와 지오메트리 추출

**Files:**
- Create: `plugin/AdaptiveMold.Core/Validation.cs`
- Create: `plugin/AdaptiveMold.Tests/ValidationTests.cs`

**Interfaces:**
- Produces: `Validation.PositiveOrDefault(double? v, double def)`, `Validation.NonNegativeOrDefault(...)`, `Validation.ToBrep(GeometryBase)`, `Validation.ToSurface(GeometryBase)`, `Validation.SafeDuplicate(Brep)`.

- [ ] **Step 1: 테스트를 먼저 쓴다**

```csharp
using NUnit.Framework;
using AdaptiveMold.Core;
using Rhino.Geometry;

namespace AdaptiveMold.Tests
{
    public class ValidationTests
    {
        [Test]
        public void Negative_and_zero_fall_back_to_default_silently()
        {
            // utils.py:52-63 — 음수·0·null 을 조용히 기본값으로 되돌린다.
            // GH 파라미터 기본값만 주고 이 정규화를 생략하면 -5 가 그대로 들어와
            // 동작이 갈린다(설계 §3.5).
            Assert.That(Validation.PositiveOrDefault(-5.0, 1000.0), Is.EqualTo(1000.0));
            Assert.That(Validation.PositiveOrDefault(0.0, 1000.0), Is.EqualTo(1000.0));
            Assert.That(Validation.PositiveOrDefault(null, 1000.0), Is.EqualTo(1000.0));
            Assert.That(Validation.PositiveOrDefault(250.0, 1000.0), Is.EqualTo(250.0));
        }

        [Test]
        public void Non_negative_allows_zero()
        {
            Assert.That(Validation.NonNegativeOrDefault(0.0, 0.0), Is.EqualTo(0.0));
            Assert.That(Validation.NonNegativeOrDefault(-1.0, 0.0), Is.EqualTo(0.0));
            Assert.That(Validation.NonNegativeOrDefault(50.0, 0.0), Is.EqualTo(50.0));
        }

        [Test]
        public void Surface_becomes_brep_and_brep_stays()
        {
            var srf = new PlaneSurface(Plane.WorldXY, new Interval(-1, 1), new Interval(-1, 1));
            Assert.That(Validation.ToBrep(srf), Is.Not.Null);

            var brep = srf.ToBrep();
            Assert.That(Validation.ToBrep(brep), Is.SameAs(brep));

            Assert.That(Validation.ToBrep(null), Is.Null);
        }

        [Test]
        public void First_face_only_for_surface_extraction()
        {
            // utils.py:136 — Brep 이면 Faces[0].UnderlyingSurface() 를 쓴다.
            // 다중 face 를 물리면 Phase C 가 첫 face 만 본다는 뜻이고,
            // 그래서 경고를 새로 넣었다(파이썬 쪽 Task 4).
            var srf = new PlaneSurface(Plane.WorldXY, new Interval(-1, 1), new Interval(-1, 1));
            var brep = srf.ToBrep();
            Assert.That(Validation.ToSurface(brep), Is.Not.Null);
        }
    }
}
```

- [ ] **Step 2: 실패 확인** — `dotnet build` 에서 `Validation` 없음

- [ ] **Step 3: 구현**

```csharp
using Rhino.Geometry;

namespace AdaptiveMold.Core
{
    /// <summary>
    /// <c>utils.py</c> 의 입력 정규화·지오메트리 추출 1:1 포팅.
    ///
    /// **조용히 기본값으로 되돌리는 동작을 그대로 옮긴다.** 파이썬이 그렇게 하고
    /// 성공 기준은 "똑같다"이므로, 여기서 예외를 던지도록 "개선"하면 안 된다.
    /// </summary>
    public static class Validation
    {
        public static double PositiveOrDefault(double? value, double def)
        {
            if (!value.HasValue) return def;
            double v = value.Value;
            if (double.IsNaN(v) || double.IsInfinity(v)) return def;
            return v <= 0 ? def : v;
        }

        public static double NonNegativeOrDefault(double? value, double def)
        {
            if (!value.HasValue) return def;
            double v = value.Value;
            if (double.IsNaN(v) || double.IsInfinity(v)) return def;
            return v < 0 ? def : v;
        }

        public static Brep ToBrep(GeometryBase geo)
        {
            if (geo == null) return null;
            if (geo is Brep b) return b;
            if (geo is Surface s) return s.ToBrep();
            return null;
        }

        public static Surface ToSurface(GeometryBase geo)
        {
            if (geo == null) return null;
            if (geo is Surface s) return s;
            if (geo is Brep b && b.Faces.Count > 0) return b.Faces[0].UnderlyingSurface();
            return null;
        }

        public static Brep SafeDuplicate(Brep brep) => brep?.DuplicateBrep();
    }
}
```

> **파이썬의 `float(value)` 파싱 실패 경로는 C# 에 없다.** GH 가 이미 `double` 로 넘겨주므로 문자열이 들어올 자리가 없다. 대신 `NaN`·`Infinity` 를 기본값으로 되돌린다 — 파이썬에서는 `float('nan') <= 0` 이 `False` 라 통과하지만, 그 값이 흘러가면 전 핀이 `NaN` 이 된다. **이것은 "똑같다"에서 벗어나는 유일한 지점이므로 저널에 적는다.**

- [ ] **Step 4: 통과 확인 후 커밋**

```bash
cd plugin && dotnet build LjksAdaptiveMold.sln && cd ..
python adaptive_mold/tools/run_csharp_tests.py
git add plugin/
git commit -m "feat(core): Validation — 조용히 기본값으로 되돌리는 동작을 그대로 옮긴다

파이썬이 음수·0·None 을 조용히 기본값으로 되돌린다. 예외를 던지도록
'개선'하면 안 된다 — 성공 기준은 똑같다이다.

유일한 이탈: NaN·Infinity 를 기본값으로 되돌린다. 파이썬은 통과시키는데
그 값이 흘러가면 전 핀이 NaN 이 된다. 저널에 적었다."
```

---

## Task 9: `Optimization` — Phase B

**Files:**
- Create: `plugin/AdaptiveMold.Core/Optimization.cs`
- Create: `plugin/AdaptiveMold.Tests/OptimizationTests.cs`

**Interfaces:**
- Consumes: `Validation`, `Grid`
- Produces: `Optimization.Run(Brep target, IList<Point3d> gridPts, Plane basePlane, double width, double length, double minH, double maxH) -> (Brep positioned, string info, string branch)`

- [ ] **Step 1: 중간 지문 대조 테스트를 먼저 쓴다**

```csharp
using System.Collections.Generic;
using System.IO;
using System.Text.Json;
using NUnit.Framework;
using AdaptiveMold.Core;
using Rhino.Geometry;

namespace AdaptiveMold.Tests
{
    public class OptimizationTests
    {
        static string FixtureDir =>
            Path.Combine(TestContext.CurrentContext.TestDirectory, "fixtures");

        public static IEnumerable<string> FixtureFiles()
        {
            foreach (var p in Directory.GetFiles(FixtureDir, "T*.json"))
                yield return p;
        }

        [TestCaseSource(nameof(FixtureFiles))]
        public void Positioned_surface_matches_python(string path)
        {
            using var doc = JsonDocument.Parse(File.ReadAllText(path));
            var input = doc.RootElement.GetProperty("input");
            var prm = input.GetProperty("params");
            var expected = doc.RootElement.GetProperty("expected");

            var plane = TestSurfaceBuilder.BuildPlane(input.GetProperty("base_plane")) ?? Plane.WorldXY;
            var target = TestSurfaceBuilder.BuildSurface(input.GetProperty("surface"));

            double w = prm.GetProperty("width").GetDouble();
            double l = prm.GetProperty("length").GetDouble();
            double s = prm.GetProperty("spacing").GetDouble();
            double minH = prm.GetProperty("min_height").GetDouble();
            double maxH = prm.GetProperty("max_height").GetDouble();

            var (gridPts, _, _) = Grid.Build(plane, w, l, s);
            var (positioned, _, branch) = Optimization.Run(target, gridPts, plane, w, l, minH, maxH);

            Assert.That(branch, Is.EqualTo(expected.GetProperty("opt_branch").GetString()),
                "Phase B 가 다른 가지를 탔다");

            var fp = expected.GetProperty("positioned_fingerprint");
            if (fp.ValueKind == JsonValueKind.Null)
            {
                Assert.That(positioned, Is.Null);
                return;
            }

            var actual = GeometryFingerprint.Compute(positioned);
            Assert.Multiple(() =>
            {
                for (int i = 0; i < 3; i++)
                {
                    Assert.That(actual.BBoxMin[i],
                        Is.EqualTo(fp.GetProperty("bbox_min")[i].GetDouble()).Within(1e-6), $"bbox_min[{i}]");
                    Assert.That(actual.BBoxMax[i],
                        Is.EqualTo(fp.GetProperty("bbox_max")[i].GetDouble()).Within(1e-6), $"bbox_max[{i}]");
                }
                Assert.That(actual.Area,
                    Is.EqualTo(fp.GetProperty("area").GetDouble()).Within(1e-6), "area");
            });
        }
    }
}
```

- [ ] **Step 2: 실패 확인** — `Optimization` 없음

- [ ] **Step 3: 구현**

```csharp
using System;
using System.Collections.Generic;
using Rhino.Geometry;
using Rhino.Geometry.Intersect;

namespace AdaptiveMold.Core
{
    /// <summary>
    /// Phase B — 목표 곡면을 stroke 범위 안에 맞도록 정렬한다.
    /// <c>optimization.py</c> 의 1:1 포팅.
    ///
    /// **순차 누산 평균을 유지한다.** <c>sum(valid)/len(valid)</c> 가 delta_z 로
    /// 쓰이고 그 값이 모든 핀 높이에 실린다 — 누산 순서가 바뀌면 최하위 비트가
    /// 흔들리고 클램프 경계에서 불리언이 뒤집힌다. 병렬화·재정렬 금지.
    /// </summary>
    public static class Optimization
    {
        public const string BranchInvalidSurface = "invalid_surface";
        public const string BranchTranslateOnly = "translate_only";
        public const string BranchNoPoints = "no_points";
        public const string BranchFitNone = "fit_none";
        public const string BranchFitFailed = "fit_failed";
        public const string BranchDupFailed = "dup_failed";
        public const string BranchFull = "full";

        public static (Brep positioned, string info, string branch) Run(
            GeometryBase targetSrf, IList<Point3d> gridPts, Plane basePlane,
            double width, double length, double minHeight, double maxHeight)
        {
            var brep = Validation.ToBrep(targetSrf);
            if (brep == null)
                return (null, "optimization skipped: invalid surface", BranchInvalidSurface);

            var (rawDistances, samplePts) = MeasureDistances(gridPts, basePlane, brep);

            var valid = new List<double>();
            foreach (var d in rawDistances) if (d.HasValue) valid.Add(d.Value);

            double targetH = (minHeight + maxHeight) / 2.0;

            if (valid.Count < 3)
            {
                var pos = Validation.SafeDuplicate(brep);
                if (valid.Count > 0)
                {
                    double avgD = Mean(valid);
                    double dz = targetH - avgD;
                    pos.Transform(Transform.Translation(basePlane.ZAxis * dz));
                    return (pos, $"translate only (dz={dz:F1}mm, {valid.Count} pts)", BranchTranslateOnly);
                }
                return (pos, "no optimization (0 in-bounds points)", BranchNoPoints);
            }

            var fitRc = Plane.FitPlaneToPoints(samplePts, out Plane fitPlane);
            if (fitRc == PlaneFitResult.Failure)
            {
                var pos = Validation.SafeDuplicate(brep);
                return (pos, "plane fit failed, no optimization", BranchFitNone);
            }
            if (fitRc != PlaneFitResult.Success)
            {
                var pos = Validation.SafeDuplicate(brep);
                double avgD = Mean(valid);
                double dz = targetH - avgD;
                pos.Transform(Transform.Translation(basePlane.ZAxis * dz));
                return (pos, $"plane fit failed, translate only (dz={dz:F1}mm)", BranchFitFailed);
            }

            var positioned = Validation.SafeDuplicate(brep);
            if (positioned == null)
                return (null, "DuplicateBrep failed", BranchDupFailed);

            var currentNormal = fitPlane.Normal;
            var targetNormal = Vector3d.ZAxis;

            // FitPlaneToPoints 는 법선 부호를 보장하지 않는다. 정규화하지 않으면
            // 회전각이 실제 기울기가 아니라 (180 - 기울기)로 나온다.
            if (currentNormal * targetNormal < 0) currentNormal = -currentNormal;

            var rotAxis = Vector3d.CrossProduct(currentNormal, targetNormal);
            double tiltAngle = 0.0;

            if (rotAxis.Length > 1e-10)
            {
                rotAxis.Unitize();
                double dot = Math.Max(-1.0, Math.Min(1.0, currentNormal * targetNormal));
                double rotAngle = Math.Acos(dot);
                tiltAngle = RhinoMath.ToDegrees(rotAngle);

                var pivot = Grid.Center(basePlane, width, length);

                var rotAxisWorld =
                    basePlane.XAxis * rotAxis.X +
                    basePlane.YAxis * rotAxis.Y +
                    basePlane.ZAxis * rotAxis.Z;
                rotAxisWorld.Unitize();

                // 벡터 a 를 b 로 돌리려면 축 (a x b) 둘레로 **+각도**만큼 돌린다.
                // 옛 파이썬 구현이 -rot_angle 을 써서 기울기를 정확히 2배로
                // 키웠다(J-003). 부호를 뒤집지 말 것.
                positioned.Transform(Transform.Rotation(rotAngle, rotAxisWorld, pivot));
            }

            var (newRaw, _) = MeasureDistances(gridPts, basePlane, positioned);
            var newValid = new List<double>();
            foreach (var d in newRaw) if (d.HasValue) newValid.Add(d.Value);

            double avgNew = newValid.Count > 0 ? Mean(newValid) : targetH;
            double deltaZ = targetH - avgNew;
            positioned.Transform(Transform.Translation(basePlane.ZAxis * deltaZ));

            return (positioned, $"tilt={tiltAngle:F1}deg, dz={deltaZ:F1}mm", BranchFull);
        }

        /// <summary>순차 누산. Sum()/Average() 로 바꾸지 말 것 — 순서가 값을 정한다.</summary>
        static double Mean(IList<double> xs)
        {
            double s = 0.0;
            for (int i = 0; i < xs.Count; i++) s += xs[i];
            return s / xs.Count;
        }

        static (List<double?> raw, List<Point3d> samples) MeasureDistances(
            IList<Point3d> gridPts, Plane basePlane, Brep brep)
        {
            var normal = basePlane.ZAxis;
            var raw = new List<double?>(gridPts.Count);
            var samples = new List<Point3d>();

            foreach (var pt in gridPts)
            {
                double? hit = RayCastDistance(pt, normal, brep);
                raw.Add(hit);
                if (hit.HasValue && basePlane.RemapToPlaneSpace(pt, out Point3d local))
                    samples.Add(new Point3d(local.X, local.Y, hit.Value));
            }

            return (raw, samples);
        }

        /// <summary>
        /// 양방향 ray-cast. <c>Intersection.RayShoot</c> 는 **교점 배열**을 돌려준다 —
        /// 곡선 파라미터가 아니다(J-002 TRAP-01, 파이썬이 이것을 오용해 죽었었다).
        /// </summary>
        static double? RayCastDistance(Point3d pt, Vector3d direction, Brep brep)
        {
            var geo = new GeometryBase[] { brep };

            var hitsPos = Intersection.RayShoot(new Ray3d(pt, direction), geo, 1);
            if (hitsPos != null && hitsPos.Length > 0) return pt.DistanceTo(hitsPos[0]);

            var hitsNeg = Intersection.RayShoot(new Ray3d(pt, -direction), geo, 1);
            if (hitsNeg != null && hitsNeg.Length > 0) return pt.DistanceTo(hitsNeg[0]);

            return null;
        }
    }
}
```

> **`Plane.FitPlaneToPoints` 의 반환이 파이썬과 다르다.** 파이썬은 튜플이고 C# 은 `PlaneFitResult` + `out Plane` 이다. 파이썬의 `fit_result is None` 가지(`OPT_FIT_NONE`)는 C# 에서 `PlaneFitResult.Failure` 로 대응시킨다. **T1~T9 이 이 가지를 타지 않으므로 대조로 검증되지 않는다** — 저널에 적을 것.

- [ ] **Step 4: 통과 확인. 어긋나면 이분한다**

```bash
cd plugin && dotnet build LjksAdaptiveMold.sln && cd ..
python adaptive_mold/tools/run_csharp_tests.py
```

**bbox 만 어긋나면** 회전 부호나 pivot 을, **area 도 어긋나면** `SafeDuplicate` 나 `MeasureDistances` 를 의심한다. **`opt_branch` 가 어긋나면** 값이 아니라 경로가 다른 것이므로 그것부터 고친다.

- [ ] **Step 5: 커밋**

```bash
git add plugin/
git commit -m "feat(core): Optimization — Phase B, 순차 누산 평균을 유지한다

중간 지문(bbox·area) 9케이스와 opt_branch 대조 통과.

Sum()/Average() 로 바꾸지 않았다 — delta_z 가 모든 핀 높이에 실리므로
누산 순서가 바뀌면 클램프 경계에서 불리언이 뒤집힌다.
FitPlaneToPoints 의 법선 부호 정규화와 +rot_angle 도 그대로 옮겼다
(J-003 에서 고친 결함이다. 부호를 뒤집으면 기울기가 2배가 된다)."
```

---

## Task 10: `Extension` — Phase C

**Files:**
- Create: `plugin/AdaptiveMold.Core/Extension.cs`
- Create: `plugin/AdaptiveMold.Tests/ExtensionTests.cs`

**Interfaces:**
- Produces: `Extension.Run(GeometryBase positioned, double width, double length) -> (Brep extended, string method, string branch)`, `Extension.TangentExtrapolate(Point3d, GeometryBase, Plane) -> double?`, 상수 `Extension.MethodSurface`/`MethodTangent`.

- [ ] **Step 1: 테스트를 먼저 쓴다**

`plugin/AdaptiveMold.Tests/ExtensionTests.cs` 전체:

```csharp
using System.Collections.Generic;
using System.IO;
using System.Text.Json;
using NUnit.Framework;
using AdaptiveMold.Core;
using Rhino.Geometry;

namespace AdaptiveMold.Tests
{
    public class ExtensionTests
    {
        static string FixtureDir =>
            Path.Combine(TestContext.CurrentContext.TestDirectory, "fixtures");

        public static IEnumerable<string> FixtureFiles()
        {
            foreach (var p in Directory.GetFiles(FixtureDir, "T*.json"))
                yield return p;
        }

        [TestCaseSource(nameof(FixtureFiles))]
        public void Extended_surface_matches_python(string path)
        {
            using var doc = JsonDocument.Parse(File.ReadAllText(path));
            var input = doc.RootElement.GetProperty("input");
            var prm = input.GetProperty("params");
            var expected = doc.RootElement.GetProperty("expected");

            var plane = TestSurfaceBuilder.BuildPlane(input.GetProperty("base_plane")) ?? Plane.WorldXY;
            var target = TestSurfaceBuilder.BuildSurface(input.GetProperty("surface"));

            double w = prm.GetProperty("width").GetDouble();
            double l = prm.GetProperty("length").GetDouble();
            double s = prm.GetProperty("spacing").GetDouble();
            double minH = prm.GetProperty("min_height").GetDouble();
            double maxH = prm.GetProperty("max_height").GetDouble();

            var (gridPts, _, _) = Grid.Build(plane, w, l, s);
            var (positioned, _, _) = Optimization.Run(target, gridPts, plane, w, l, minH, maxH);
            var (extended, method, branch) = Extension.Run(positioned, w, l);

            Assert.That(branch, Is.EqualTo(expected.GetProperty("ext_branch").GetString()),
                "Phase C 가 다른 가지를 탔다");

            var fp = expected.GetProperty("extended_fingerprint");
            if (fp.ValueKind == JsonValueKind.Null)
            {
                Assert.That(extended, Is.Null);
                return;
            }

            var actual = GeometryFingerprint.Compute(extended);
            Assert.Multiple(() =>
            {
                for (int i = 0; i < 3; i++)
                {
                    Assert.That(actual.BBoxMin[i],
                        Is.EqualTo(fp.GetProperty("bbox_min")[i].GetDouble()).Within(1e-6), $"bbox_min[{i}]");
                    Assert.That(actual.BBoxMax[i],
                        Is.EqualTo(fp.GetProperty("bbox_max")[i].GetDouble()).Within(1e-6), $"bbox_max[{i}]");
                }
            });
        }
    }
}
```

- [ ] **Step 2: 실패 확인**

- [ ] **Step 3: 구현**

```csharp
using System;
using Rhino.Geometry;
using Rhino.Geometry.Intersect;

namespace AdaptiveMold.Core
{
    /// <summary>
    /// Phase C — positioned 곡면을 몰드 전체로 확장한다. <c>extension.py</c> 1:1 포팅.
    /// </summary>
    public static class Extension
    {
        public const string MethodSurface = "surface_extend";
        public const string MethodTangent = "tangent_fallback";

        public const string BranchNoSurface = "no_surface";
        public const string BranchSurfaceExtend = "surface_extend";
        public const string BranchTangentFallback = "tangent_fallback";

        public static (Brep extended, string method, string branch) Run(
            GeometryBase positionedSrf, double width, double length)
        {
            var srf = Validation.ToSurface(positionedSrf);
            if (srf == null)
                return (Validation.ToBrep(positionedSrf), MethodTangent, BranchNoSurface);

            double extensionLength = Math.Max(width, length) * 1.5;

            try
            {
                var extended = srf;
                extended = TryExtend(extended, IsoStatus.South, extensionLength);
                extended = TryExtend(extended, IsoStatus.North, extensionLength);
                extended = TryExtend(extended, IsoStatus.West, extensionLength);
                extended = TryExtend(extended, IsoStatus.East, extensionLength);

                // 파이썬은 `extended is not srf` — **참조 동등성**이다.
                // == 나 Equals 로 바꾸면 의미가 달라진다.
                if (extended != null && !ReferenceEquals(extended, srf))
                {
                    var b = extended.ToBrep();
                    if (b != null) return (b, MethodSurface, BranchSurfaceExtend);
                }
            }
            catch (Exception)
            {
                // 파이썬이 통째로 삼킨다. 그대로 옮긴다.
            }

            return (Validation.ToBrep(positionedSrf), MethodTangent, BranchTangentFallback);
        }

        static Surface TryExtend(Surface surface, IsoStatus iso, double length)
        {
            if (surface == null) return null;
            try
            {
                var result = surface.Extend(iso, length, true);
                if (result != null) return result;
            }
            catch (Exception) { }
            return surface;
        }

        /// <summary>
        /// 곡면 밖 점의 높이를 접평면 외삽으로 구한다.
        ///
        /// <c>Brep.ClosestPoint(Point3d)</c> 는 C# 에서 **구조체**를 돌려주므로
        /// 파이썬의 <c>cp is None</c> 가지가 여기서는 사라진다. 없어지는 가지를
        /// 적어 둔다 — 나중에 "왜 가지 수가 다르지"가 된다.
        /// </summary>
        public static double? TangentExtrapolate(Point3d gridPt, GeometryBase positionedSrf, Plane basePlane)
        {
            var brep = Validation.ToBrep(positionedSrf);
            if (brep == null) return null;

            var closestPt = brep.ClosestPoint(gridPt);

            var srfNormal = SurfaceNormalAt(positionedSrf, closestPt) ?? basePlane.ZAxis;
            var tangentPlane = new Plane(closestPt, srfNormal);

            var normal = basePlane.ZAxis;
            var line = new Line(gridPt, gridPt + normal * 10000.0);

            if (Intersection.LinePlane(line, tangentPlane, out double t))
            {
                var hitPt = line.PointAt(t);
                var vec = hitPt - gridPt;
                return Math.Abs(vec * normal);
            }

            return null;
        }

        static Vector3d? SurfaceNormalAt(GeometryBase geo, Point3d point)
        {
            var srf = Validation.ToSurface(geo);
            if (srf == null) return null;
            if (srf.ClosestPoint(point, out double u, out double v))
            {
                var n = srf.NormalAt(u, v);
                if (n.IsValid) return n;
            }
            return null;
        }
    }
}
```

- [ ] **Step 4: 통과 확인 후 커밋**

```bash
cd plugin && dotnet build LjksAdaptiveMold.sln && cd ..
python adaptive_mold/tools/run_csharp_tests.py
git add plugin/
git commit -m "feat(core): Extension — Phase C

extended_fingerprint 와 ext_branch 9케이스 대조 통과.

옮기면서 적어 둔 것 둘:
- 파이썬의 `extended is not srf` 는 참조 동등성이라 ReferenceEquals 로
  옮겼다. ==/Equals 는 의미가 다르다.
- Brep.ClosestPoint 가 C# 에서는 구조체라 파이썬의 cp is None 가지가
  사라진다. 가지 수가 한 개 줄어드는 것이 정상이다."
```

---

## Task 11: `Projection` — Phase D, 배열 5개를 직접 대조

**Files:**
- Create: `plugin/AdaptiveMold.Core/Projection.cs`
- Create: `plugin/AdaptiveMold.Tests/ProjectionTests.cs`

**Interfaces:**
- Produces: `Projection.CalculateHeights(IList<Point3d> gridPts, GeometryBase positioned, GeometryBase extended, Plane basePlane, double minH, double maxH, string extensionMethod, IList<string> branchOut) -> (List<double>, List<bool>, List<bool>)` + `Branch*` 상수 7개.

- [ ] **Step 1: 테스트를 먼저 쓴다 — 여기가 완료조건 3·4다**

`plugin/AdaptiveMold.Tests/ProjectionTests.cs` — 파일 골격은 Phase 2 머리말의 것을 쓰고 아래 메서드를 넣는다.

```csharp
        [TestCaseSource(nameof(FixtureFiles))]
        public void Heights_flags_and_branches_match_python(string path)
        {
            using var doc = JsonDocument.Parse(File.ReadAllText(path));
            var input = doc.RootElement.GetProperty("input");
            var prm = input.GetProperty("params");
            var expected = doc.RootElement.GetProperty("expected");

            var plane = TestSurfaceBuilder.BuildPlane(input.GetProperty("base_plane")) ?? Plane.WorldXY;
            var target = TestSurfaceBuilder.BuildSurface(input.GetProperty("surface"));

            double w = prm.GetProperty("width").GetDouble();
            double l = prm.GetProperty("length").GetDouble();
            double s = prm.GetProperty("spacing").GetDouble();
            double minH = prm.GetProperty("min_height").GetDouble();
            double maxH = prm.GetProperty("max_height").GetDouble();

            var (gridPts, _, _) = Grid.Build(plane, w, l, s);
            var (positioned, _, _) = Optimization.Run(target, gridPts, plane, w, l, minH, maxH);
            var (extended, method, _) = Extension.Run(positioned, w, l);

            var branches = new List<string>();
            var (heights, clamps, exts) = Projection.CalculateHeights(
                gridPts, positioned, extended, plane, minH, maxH, method, branches);

            var eH = expected.GetProperty("pin_heights");
            var eC = expected.GetProperty("clamp_flags");
            var eE = expected.GetProperty("extension_flags");
            var eB = expected.GetProperty("branch_taken");

            Assert.That(heights.Count, Is.EqualTo(eH.GetArrayLength()), "핀 개수");

            Assert.Multiple(() =>
            {
                for (int i = 0; i < heights.Count; i++)
                {
                    Assert.That(heights[i], Is.EqualTo(eH[i].GetDouble()).Within(1e-6), $"pin_heights[{i}]");
                    // 플래그와 가지는 허용오차가 없다. 어긋나면 다른 경로를 탄 것이다.
                    Assert.That(clamps[i], Is.EqualTo(eC[i].GetBoolean()), $"clamp_flags[{i}]");
                    Assert.That(exts[i], Is.EqualTo(eE[i].GetBoolean()), $"extension_flags[{i}]");
                    Assert.That(branches[i], Is.EqualTo(eB[i].GetString()), $"branch_taken[{i}]");
                }
            });
        }
```

- [ ] **Step 2: 실패 확인**

- [ ] **Step 3: 구현**

```csharp
using System;
using System.Collections.Generic;
using Rhino.Geometry;
using Rhino.Geometry.Intersect;

namespace AdaptiveMold.Core
{
    /// <summary>
    /// Phase D — 격자점마다 높이를 산출하고 stroke 범위로 클램핑한다.
    /// <c>projection.py</c> 1:1 포팅. **가장 위험한 모듈이다** — 폴백이 4단계이고,
    /// 어느 가지를 탔는지가 값으로는 드러나지 않는다.
    ///
    /// 가지 문자열은 파이썬과 **정확히 일치**해야 한다.
    /// </summary>
    public static class Projection
    {
        public const string BranchRayPosPanel = "ray+/panel";
        public const string BranchRayNegPanel = "ray-/panel";
        public const string BranchRayPosExt = "ray+/ext";
        public const string BranchRayNegExt = "ray-/ext";
        public const string BranchTangent = "tangent";
        public const string BranchClosest = "closest";
        public const string BranchDefault = "default";

        public static (List<double> heights, List<bool> clamps, List<bool> extensions) CalculateHeights(
            IList<Point3d> gridPts, GeometryBase positionedSrf, GeometryBase extendedSrf,
            Plane basePlane, double minHeight, double maxHeight, string extensionMethod,
            IList<string> branchOut = null)
        {
            var positionedBrep = Validation.ToBrep(positionedSrf);
            var extendedBrep = Validation.ToBrep(extendedSrf);

            var normal = basePlane.ZAxis;
            var heights = new List<double>(gridPts.Count);
            var clamps = new List<bool>(gridPts.Count);
            var extensions = new List<bool>(gridPts.Count);

            foreach (var pt in gridPts)
            {
                double? h = null;
                bool isExtension;
                string branch = null;

                if (positionedBrep != null)
                {
                    var (hit, sign) = RayCastHeight(pt, normal, positionedBrep);
                    h = hit;
                    if (h.HasValue)
                        branch = sign == "+" ? BranchRayPosPanel : BranchRayNegPanel;
                }

                if (h.HasValue)
                {
                    isExtension = false;
                }
                else
                {
                    isExtension = true;

                    // Phase C 가 "Surface.Extend 였나 tangent 폴백이었나"를 여기로
                    // 실어 보낸다. 이 결합 때문에 C·D 를 캔버스에서 나누지 않는다.
                    if (extendedBrep != null && extensionMethod != Extension.MethodTangent)
                    {
                        var (hit, sign) = RayCastHeight(pt, normal, extendedBrep);
                        h = hit;
                        if (h.HasValue)
                            branch = sign == "+" ? BranchRayPosExt : BranchRayNegExt;
                    }

                    if (!h.HasValue && positionedBrep != null)
                    {
                        h = Extension.TangentExtrapolate(pt, positionedBrep, basePlane);
                        if (h.HasValue) branch = BranchTangent;
                    }

                    if (!h.HasValue)
                    {
                        h = ClosestPointHeight(pt, normal, extendedBrep ?? positionedBrep);
                        if (h.HasValue) branch = BranchClosest;
                    }
                }

                if (!h.HasValue)
                {
                    h = (minHeight + maxHeight) / 2.0;
                    isExtension = true;
                    branch = BranchDefault;
                }

                var (clamped, didClamp) = Clamp(h.Value, minHeight, maxHeight);
                heights.Add(clamped);
                clamps.Add(didClamp);
                extensions.Add(isExtension);
                branchOut?.Add(branch);
            }

            return (heights, clamps, extensions);
        }

        static (double? height, string sign) RayCastHeight(Point3d pt, Vector3d direction, Brep brep)
        {
            if (brep == null) return (null, null);

            var geo = new GeometryBase[] { brep };

            var hitsPos = Intersection.RayShoot(new Ray3d(pt, direction), geo, 1);
            if (hitsPos != null && hitsPos.Length > 0) return (pt.DistanceTo(hitsPos[0]), "+");

            var hitsNeg = Intersection.RayShoot(new Ray3d(pt, -direction), geo, 1);
            if (hitsNeg != null && hitsNeg.Length > 0) return (pt.DistanceTo(hitsNeg[0]), "-");

            return (null, null);
        }

        static double? ClosestPointHeight(Point3d pt, Vector3d normal, Brep brep)
        {
            if (brep == null) return null;
            var cp = brep.ClosestPoint(pt);   // 구조체 — null 이 될 수 없다
            var vec = cp - pt;
            return Math.Abs(vec * normal);
        }

        /// <summary>높이가 항상 양수인 것은 파이썬과 같다 — 부호 소실은 그대로 옮긴다.</summary>
        static (double h, bool clamped) Clamp(double h, double minHeight, double maxHeight)
        {
            if (h < minHeight) return (minHeight, true);
            if (h > maxHeight) return (maxHeight, true);
            return (h, false);
        }
    }
}
```

- [ ] **Step 4: 통과 확인. 이것이 완료조건 3·4다**

```bash
cd plugin && dotnet build LjksAdaptiveMold.sln && cd ..
python adaptive_mold/tools/run_csharp_tests.py
```

**허용오차를 키워서 통과시키지 않는다.** 플래그·가지는 예외가 없다. 어긋나면 `1e-6` 을 조정하기 전에 **어느 핀이 어느 가지에서 갈렸는지** 먼저 본다.

- [ ] **Step 5: 커밋**

```bash
git add plugin/
git commit -m "feat(core): Projection — Phase D, 완료조건 3·4

핀 높이 1e-6, clamp_flags·extension_flags·branch_taken 정확 일치.
9케이스 전부.

Phase C 가 extension_method 를 Phase D 의 분기에 실어 보내는 결합을
그대로 옮겼다 — 이 결합 때문에 C·D 를 캔버스에서 나누지 않는다.
부호 소실(abs)도 그대로다. 성공 기준은 옳음이 아니라 같음이다."
```

---

## Task 12: `MoldSolver` — 계약과 전체 파이프라인

**Files:**
- Create: `plugin/AdaptiveMold.Core/MoldSolver.cs`
- Create: `plugin/AdaptiveMold.Tests/MoldSolverTests.cs`

**Interfaces:**
- Produces: `MoldSolver.Run(GeometryBase target, Plane? basePlane, double width, double length, double spacing, double maxHeight, double minHeight, bool compute) -> MoldResult`. **M3 어댑터가 이것만 부른다.**

- [ ] **Step 1: 계약 테스트를 먼저 쓴다**

`plugin/AdaptiveMold.Tests/MoldSolverTests.cs` — 파일 골격은 Phase 2 머리말의 것을 쓰고 아래 메서드들을 넣는다.

```csharp
        [Test]
        public void Compute_false_returns_empty_lists_not_null()
        {
            var r = MoldSolver.Run(null, null, 1000, 1000, 200, 400, 0, compute: false);

            Assert.That(r.PinHeights, Is.Empty);
            Assert.That(r.GridPts, Is.Empty);
            Assert.That(r.Info, Does.Contain("Compute is disabled"));
            Assert.That(r.Messages, Has.Some.Matches<MoldMessage>(
                m => m.Level == MoldMessageLevel.Remark && m.Text.Contains("compute")));
        }

        [Test]
        public void Min_ge_max_is_an_error_not_an_exception()
        {
            var srf = new PlaneSurface(Plane.WorldXY, new Interval(-500, 500), new Interval(-500, 500));
            var r = MoldSolver.Run(srf.ToBrep(), null, 1000, 1000, 200, 100, 100, compute: true);

            Assert.That(r.PinHeights, Is.Empty);
            Assert.That(r.Messages, Has.Some.Matches<MoldMessage>(
                m => m.Level == MoldMessageLevel.Error));
        }

        [Test]
        public void Negative_width_falls_back_silently()
        {
            var srf = new PlaneSurface(Plane.WorldXY, new Interval(-2000, 2000), new Interval(-2000, 2000));
            var r = MoldSolver.Run(srf.ToBrep(), null, -5, 1000, 200, 400, 0, compute: true);

            // width -5 -> 1000 이므로 nx = floor(1000/200)+1 = 6
            Assert.That(r.Nx, Is.EqualTo(6));
        }
```

그리고 **전 케이스 end-to-end 대조**:

```csharp
        [TestCaseSource(nameof(FixtureFiles))]
        public void Full_pipeline_matches_python(string path)
        {
            using var doc = JsonDocument.Parse(File.ReadAllText(path));
            var input = doc.RootElement.GetProperty("input");
            var prm = input.GetProperty("params");
            var expected = doc.RootElement.GetProperty("expected");

            Plane? plane = TestSurfaceBuilder.BuildPlane(input.GetProperty("base_plane"));
            var target = TestSurfaceBuilder.BuildSurface(input.GetProperty("surface"));

            var r = MoldSolver.Run(target, plane,
                prm.GetProperty("width").GetDouble(),
                prm.GetProperty("length").GetDouble(),
                prm.GetProperty("spacing").GetDouble(),
                prm.GetProperty("max_height").GetDouble(),
                prm.GetProperty("min_height").GetDouble(),
                compute: true);

            var eH = expected.GetProperty("pin_heights");
            var eT = expected.GetProperty("pin_tops");

            Assert.Multiple(() =>
            {
                for (int i = 0; i < r.PinHeights.Count; i++)
                {
                    Assert.That(r.PinHeights[i], Is.EqualTo(eH[i].GetDouble()).Within(1e-6), $"h[{i}]");
                    Assert.That(r.PinTops[i].X, Is.EqualTo(eT[i][0].GetDouble()).Within(1e-6), $"top[{i}].X");
                    Assert.That(r.PinTops[i].Y, Is.EqualTo(eT[i][1].GetDouble()).Within(1e-6), $"top[{i}].Y");
                    Assert.That(r.PinTops[i].Z, Is.EqualTo(eT[i][2].GetDouble()).Within(1e-6), $"top[{i}].Z");
                }
                Assert.That(r.OptBranch, Is.EqualTo(expected.GetProperty("opt_branch").GetString()));
                Assert.That(r.ExtBranch, Is.EqualTo(expected.GetProperty("ext_branch").GetString()));
            });
        }

        [TestCaseSource(nameof(FixtureFiles))]
        public void Messages_match_python(string path)
        {
            using var doc = JsonDocument.Parse(File.ReadAllText(path));
            var input = doc.RootElement.GetProperty("input");
            var prm = input.GetProperty("params");
            var expected = doc.RootElement.GetProperty("expected").GetProperty("messages");

            Plane? plane = TestSurfaceBuilder.BuildPlane(input.GetProperty("base_plane"));
            var target = TestSurfaceBuilder.BuildSurface(input.GetProperty("surface"));

            var r = MoldSolver.Run(target, plane,
                prm.GetProperty("width").GetDouble(), prm.GetProperty("length").GetDouble(),
                prm.GetProperty("spacing").GetDouble(), prm.GetProperty("max_height").GetDouble(),
                prm.GetProperty("min_height").GetDouble(), compute: true);

            Assert.That(r.Messages.Count, Is.EqualTo(expected.GetArrayLength()),
                "메시지 개수가 다르다 — 등급이나 문안이 아니라 '몇 개 올라가는가'부터 맞춰야 한다");

            for (int i = 0; i < r.Messages.Count; i++)
            {
                Assert.That(r.Messages[i].Level.ToString(),
                    Is.EqualTo(expected[i][0].GetString()), $"messages[{i}].level");
                Assert.That(r.Messages[i].Text,
                    Is.EqualTo(expected[i][1].GetString()), $"messages[{i}].text");
            }
        }
```

> **메시지 대조는 파이썬의 `AddRuntimeMessage` 레벨 문자열과 C# `MoldMessageLevel` 의 `ToString()` 이 같아야 성립한다.** Task 4 Step 1에서 확인한 실제 값에 맞춰 `_MessageCollector` 가 `"Warning"`/`"Error"`/`"Remark"` 로 정규화하도록 맞춘다.

- [ ] **Step 2: 실패 확인**

- [ ] **Step 3: 구현**

```csharp
using System;
using System.Collections.Generic;
using System.Text;
using Rhino.Geometry;

namespace AdaptiveMold.Core
{
    /// <summary>
    /// <c>run_adaptive_mold</c> 에서 Phase E 를 뺀 것.
    ///
    /// 여기 있는 것은 "조립"이 아니라 **계약**이다 — 입력 정규화, min>=max 의
    /// 에러 종료, compute=false 의 빈 결과, pin_tops 산식, 리포트 문자열.
    /// 이것들이 어댑터로 흩어지면 대조가 불가능해진다(설계 §3.2).
    /// </summary>
    public static class MoldSolver
    {
        public static MoldResult Run(
            GeometryBase targetSrf, Plane? basePlaneOrNull,
            double width, double length, double spacing,
            double maxHeight, double minHeight, bool compute)
        {
            var result = new MoldResult();

            if (!compute)
            {
                result.Info = "Compute is disabled. Set compute=True to run.";
                result.AddRemark("compute 가 꺼져 있다. 계산하려면 Boolean Toggle 을 True 로.");
                return result;
            }

            var basePlane = basePlaneOrNull ?? Plane.WorldXY;

            width = Validation.PositiveOrDefault(width, 1000.0);
            length = Validation.PositiveOrDefault(length, 1000.0);
            spacing = Validation.PositiveOrDefault(spacing, 200.0);
            maxHeight = Validation.PositiveOrDefault(maxHeight, 400.0);
            minHeight = Validation.NonNegativeOrDefault(minHeight, 0.0);

            if (minHeight >= maxHeight)
            {
                result.AddError($"min_height ({minHeight}) 가 max_height ({maxHeight}) 보다 "
                              + "크거나 같다. 계산하지 않았다. 두 값을 확인할 것.");
                return result;
            }

            if (targetSrf == null)
            {
                result.AddError("target_srf 가 비어 있다. Surface 또는 Brep 을 물릴 것.");
                return result;
            }

            var targetBrep = Validation.ToBrep(targetSrf);
            if (targetBrep == null)
            {
                result.AddError("target_srf 에서 Brep 을 얻지 못했다. Surface 또는 Brep 을 "
                              + "물릴 것 (Mesh·Curve·Point 는 받지 않는다).");
                return result;
            }

            // --- Phase A ---
            List<Point3d> gridPts;
            int nx, ny;
            try
            {
                (gridPts, nx, ny) = Grid.Build(basePlane, width, length, spacing);
            }
            catch (ArgumentException e)
            {
                result.AddError(e.Message);
                return result;
            }

            result.GridPts = gridPts;
            result.Nx = nx;
            result.Ny = ny;
            int totalPins = nx * ny;

            double remX = width - (nx - 1) * spacing;
            double remY = length - (ny - 1) * spacing;
            if (remX > 1e-9 || remY > 1e-9)
            {
                result.AddRemark(
                    $"spacing {spacing:G} 이 몰드 크기를 나누지 못한다. 핀은 X 방향 "
                    + $"{(nx - 1) * spacing:G}mm, Y 방향 {(ny - 1) * spacing:G}mm 까지만 덮는다 "
                    + $"(나머지 {remX:G}/{remY:G}mm 는 비어 있다).");
            }

            // --- Phase B ---
            var (positioned, optInfo, optBranch) = Optimization.Run(
                targetBrep, gridPts, basePlane, width, length, minHeight, maxHeight);
            result.OptBranch = optBranch;

            if (positioned == null)
            {
                result.AddError("Phase B 정렬이 실패했다 (DuplicateBrep 실패). "
                              + "곡면이 유효한지 확인할 것.");
                return result;
            }

            // --- Phase C ---
            if (positioned.Faces.Count > 1)
            {
                result.AddWarning($"target_srf 의 face 가 {positioned.Faces.Count} 개다. "
                    + "곡면 확장(Phase C)은 첫 face 만 쓰므로 결과가 나머지 face 를 "
                    + "반영하지 않는다. 단일 face 로 합치거나 face 마다 따로 돌릴 것.");
            }

            var (extended, extMethod, extBranch) = Extension.Run(positioned, width, length);
            result.ExtBranch = extBranch;

            // --- Phase D ---
            var (heights, clamps, exts) = Projection.CalculateHeights(
                gridPts, positioned, extended, basePlane,
                minHeight, maxHeight, extMethod, result.BranchTaken);

            result.PinHeights = heights;
            result.ClampFlags = clamps;
            result.ExtensionFlags = exts;

            int nClamped = 0;
            foreach (var c in clamps) if (c) nClamped++;
            if (nClamped > 0)
            {
                result.AddWarning($"핀 {nClamped}/{totalPins} 개가 행정 한계에 걸렸다. "
                    + "그 지점은 목표 곡면을 재현하지 못한다. clamp_flags 로 위치를 "
                    + "확인하고, max_height 를 늘리거나 곡면을 완만하게 할 것.");
            }

            // pin_tops — housing 높이 0 기준 (설계 §3.5). 파이썬은
            // transformation.get_housing_height 를 더하지만 그 모듈은 범위 밖이다.
            var normal = basePlane.ZAxis;
            result.PinTops = new List<Point3d>(gridPts.Count);
            for (int i = 0; i < gridPts.Count; i++)
                result.PinTops.Add(gridPts[i] + normal * heights[i]);

            result.Info = BuildReport(nx, ny, width, length, optInfo, extMethod,
                                      heights, clamps, exts, totalPins);

            result.AddRemark($"AdaptiveMold v1 계산 완료. 핀 {totalPins}개.");
            return result;
        }

        /// <summary>
        /// 리포트 문자열. **대조 대상이 아니다**(설계 §5.1) — 파이썬 <c>{:.0f}</c> 는
        /// 짝수 반올림, .NET <c>F0</c> 은 away-from-zero 라 .5 경계에서 갈린다.
        /// </summary>
        static string BuildReport(int nx, int ny, double width, double length,
            string optInfo, string extMethod, IList<double> heights,
            IList<bool> clamps, IList<bool> exts, int totalPins)
        {
            int nClamp = 0; foreach (var c in clamps) if (c) nClamp++;
            int nExt = 0; foreach (var e in exts) if (e) nExt++;

            double hMin = double.MaxValue, hMax = double.MinValue;
            foreach (var h in heights) { if (h < hMin) hMin = h; if (h > hMax) hMax = h; }
            if (heights.Count == 0) { hMin = 0; hMax = 0; }

            var sb = new StringBuilder();
            sb.AppendLine($"Mold:      {width:F0} x {length:F0} mm,  {nx} x {ny} = {totalPins} pins");
            sb.AppendLine($"Surface:   {optInfo}");
            sb.AppendLine($"Extension: {extMethod}");
            sb.AppendLine($"Coverage:  {totalPins - nExt} / {totalPins} 곡면 위,  {nExt} / {totalPins} 확장영역");
            sb.AppendLine($"Heights:   {hMin:F1} ~ {hMax:F1} mm");
            sb.Append($"Clamped:   {nClamp} / {totalPins}");
            return sb.ToString();
        }
    }
}
```

> `BuildReport` 의 형식은 파이썬 `_build_report` 를 **읽고 맞춘다.** 위 코드는 골격이므로 `sed -n '197,225p' adaptive_mold/src/adaptive_mold_v1.py` 로 실제 줄 구성을 확인해 옮길 것. **대조하지 않으므로 완전히 같을 필요는 없지만**, 사용자가 보는 것이므로 항목 순서는 맞춘다.

- [ ] **Step 4: 통과 확인 — 전 케이스 end-to-end**

```bash
cd plugin && dotnet build LjksAdaptiveMold.sln && cd ..
python adaptive_mold/tools/run_csharp_tests.py
```

**메시지 대조가 어긋나면 개수부터 맞춘다.** 등급·문안은 그다음이다.

- [ ] **Step 5: 성능을 잰다 — 완료조건 5**

```csharp
        [Test]
        public void T8_is_under_500ms_median()
        {
            var path = Path.Combine(FixtureDir, "T8_large_grid.json");
            using var doc = JsonDocument.Parse(File.ReadAllText(path));
            var input = doc.RootElement.GetProperty("input");
            var prm = input.GetProperty("params");
            var target = TestSurfaceBuilder.BuildSurface(input.GetProperty("surface"));

            double w = prm.GetProperty("width").GetDouble();
            double l = prm.GetProperty("length").GetDouble();
            double s = prm.GetProperty("spacing").GetDouble();
            double minH = prm.GetProperty("min_height").GetDouble();
            double maxH = prm.GetProperty("max_height").GetDouble();

            // 워밍업 1회 — 첫 호출은 프로세스 1회성 비용을 포함한다(J-015 Q1 실측).
            MoldSolver.Run(target, null, w, l, s, maxH, minH, true);

            var times = new List<double>();
            for (int i = 0; i < 5; i++)
            {
                var sw = System.Diagnostics.Stopwatch.StartNew();
                MoldSolver.Run(target, null, w, l, s, maxH, minH, true);
                sw.Stop();
                times.Add(sw.Elapsed.TotalMilliseconds);
            }
            times.Sort();
            double median = times[times.Count / 2];

            TestContext.WriteLine($"T8 median = {median:F1} ms  (all: {string.Join(", ", times)})");
            Assert.That(median, Is.LessThan(500.0),
                "파이썬 실측이 25~49ms 다. 500ms 를 넘으면 회귀다.");
        }
```

- [ ] **Step 6: 커밋**

```bash
git add plugin/
git commit -m "feat(core): MoldSolver — 계약과 전체 파이프라인, 완료조건 3·4·5

전 케이스 end-to-end 대조 통과. pin_heights·pin_tops 1e-6,
clamp/extension/branch 정확 일치, opt_branch·ext_branch 일치,
messages 개수·등급·문안 일치.

조립이 아니라 계약을 담는다 — 입력 정규화, min>=max 에러 종료,
compute=false 빈 결과, pin_tops 산식(housing 0 기준), 리포트.
이것들이 어댑터로 흩어지면 대조가 불가능해진다.

T8 중앙값을 워밍업 1 + 측정 5 로 재고 500ms 게이트를 걸었다."
```

---

## Task 13: M2b 마감 — 저널과 완료 판정

**Files:**
- Create: `docs/journal/J-016-M2b-core-port.md`
- Modify: `docs/superpowers/specs/2026-08-14-amv1-gha-yak-design.md`

- [ ] **Step 1: 저널 번호를 확인한다**

```bash
git ls-tree --name-only feature/unfold-v1 docs/journal/ | tail -5
git ls-tree --name-only feature/gha-port docs/journal/ | tail -5
```

`J-016` 이 양쪽에 없어야 한다. 있으면 다음 번호로.

- [ ] **Step 2: 저널을 쓴다**

`docs/journal/README.md` 의 네 종류만. 이 마일스톤에서 **반드시 적을 것**:

- **FACT** — 폴백 가지 커버리지(T9 추가 후 몇/7 인지), Phase B·C 가지 커버리지, T8 C# 실측(중앙값과 5회 원값), 허용오차를 조정했다면 그 값과 이유.
- **DECISION** — 중간 산출물 지문을 정본으로 올린 것(스펙 §5.5 를 뒤집었다), `NaN` 처리가 파이썬과 다른 것.
- **TRAP** — 포팅 중 실제로 걸린 것만. RhinoCommon API 시그니처 차이(`FitPlaneToPoints`·`RemapToPlaneSpace`·`ClosestPoint`·`LinePlane`)는 **겪었을 때만** 적는다.
- **미해결** — 여전히 미커버인 가지, `info` 대조 제외, Phase E.

- [ ] **Step 3: 완료 조건을 확인한다**

스펙 §8 기준:

- [ ] 3. 골든 픽스처 T1~T9 를 허용오차 안에서 재현하고 **플래그는 정확히 일치**
- [ ] 4. **폴백 가지가 핀별로 일치** — 커버된 가지에 한해. 커버리지 숫자를 저널에 명시
- [ ] 5. T8 중앙값 ≤ 500 ms, `info` 가 경과 ms 보고 *(경과 ms 는 M3 에서 `info` 에 넣는다 — 여기서는 측정만)*

- [ ] **Step 4: 스펙의 M2b 행을 완료로 바꾸고 커밋**

```bash
git add docs/
git commit -m "docs(journal): J-016 마감 — M2b Core 포팅 완료

Phase A~D 가 파이썬과 값·플래그·가지·메시지까지 일치한다.
다음은 M3 — GH 어댑터 배선(입력 8/출력 8), 툴팁 정본 이관,
예제 .gh 4개, 매뉴얼, 자동검사 5개, Yak 매니페스트."
```

---

## 이 계획이 다루지 않는 것

| 항목 | 어디서 |
|---|---|
| GH 어댑터 배선 (입력 8 / 출력 8), `nx`·`ny` 출력 | M3 |
| 툴팁 정본 `docs/params.ko.json` 이관, 신규 문안 6개 | M3 |
| 우클릭 메뉴(`예제 열기`·`매뉴얼 열기`) — F1 이 죽어 있어 **필수** | M3 |
| 예제 `.gh` 4개(대조 예제 `05_parity.gh` 포함), 매뉴얼 | M3 |
| 도움말 자동검사 5개 | M3 |
| Yak 매니페스트·릴리스 절차·아이콘 | M3 |
| `info` 에 경과 ms 싣기 | M3 |
| `AMv1 Inspect` 를 `.gha` 위에 얹어 `platform_path` 제거 | M4 |
| Phase E, 픽스처 나머지 한계(`closest`·`default`, 부호) | 스펙 §11 |
